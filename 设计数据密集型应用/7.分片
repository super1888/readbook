# 《设计数据密集型应用》读书笔记

## 第 7 章：分片

### 本章核心
这一章讲的是分片，也可以理解为分区：当单台机器无法承载全部数据或全部读写请求时，把数据按照某种规则拆分到多台机器上。对 Java 后端开发来说，最熟悉的场景是 MySQL 数据量过大后的水平拆分、分库分表，以及 Redis Cluster 通过哈希槽把 key 分散到多个节点上。

分片的核心不是“把表拆开”这么简单，而是要回答几个问题：数据按照什么规则拆？请求如何找到对应分片？热点数据怎么办？扩容时数据如何迁移？跨分片查询和事务如何处理？

### 我读完后的直观感受
这一章相对容易理解，因为很多 Java 开发都听过甚至用过分库分表。比如订单表数据太大，就按用户 ID 或订单 ID 拆成多张表；Redis 单机内存不够，就用 Cluster，把不同 key 分布到不同槽位和节点上。

但读完这一章会发现，分片不是一个单纯的性能优化手段，而是一种会深刻影响数据建模、查询方式、事务边界、运维扩容和业务代码复杂度的架构选择。分片可以解决数据量和吞吐问题，但也会带来跨分片查询、热点、再平衡、路由和一致性问题。

所以分片不是越早做越好，也不是所有大表都必须立刻拆。真正重要的是理解业务访问模式，并选择一个能长期支撑主要查询路径的分片键。

### 关键概念

#### 1. 为什么需要分片
复制解决的是多副本问题，而分片解决的是单个节点放不下或扛不住的问题。

常见原因包括：
- 单表数据量太大，索引和查询性能下降。
- 单库写入压力过高，主库成为瓶颈。
- 单机磁盘、内存或 CPU 不够。
- Redis 单节点内存不足。
- 大量请求集中在一个节点上，无法继续水平扩展。

在 MySQL 中，常见表现是单表数据达到几千万甚至上亿后，查询、DDL、备份、索引维护都变慢。此时可以考虑水平拆表，把一张大表拆成多张结构相同的小表。

在 Redis 中，如果单节点内存或 QPS 达到瓶颈，就可以通过 Redis Cluster 把 key 分散到多个节点上，每个节点只负责一部分槽位。

#### 2. 分片与复制的区别
复制是多个节点保存同一份数据的副本，目标是提高可用性和读能力。

分片是多个节点各自保存不同的数据子集，目标是提高容量和吞吐。

两者通常会组合使用。例如一个 MySQL 分库分表系统中，每个分片本身还可以有主从复制；Redis Cluster 中，每个 master 负责一部分槽位，同时也可以配置 replica 提高可用性。

可以简单理解为：
- 复制解决“同一份数据多放几份”。
- 分片解决“不同数据分开放”。
- 真实系统往往既需要分片，也需要复制。

#### 3. 分片键
分片键是决定一条数据应该放在哪个分片上的字段。

例如订单表可以按 `user_id` 分片，也可以按 `order_id` 分片。

分片键选择非常关键，因为它会直接影响：
- 数据是否均匀分布。
- 查询是否能命中单个分片。
- 是否容易产生热点。
- 是否支持主要业务查询。
- 后续扩容和迁移是否方便。

比如按 `user_id` 分片，查询某个用户的订单非常方便，但按订单号查询时可能不知道去哪个分片。按 `order_id` 分片，订单详情查询方便，但用户订单列表可能需要额外索引或跨分片查询。

这说明分片键没有绝对正确答案，必须围绕最核心、最高频、最不能慢的访问路径来设计。

#### 4. 基于范围分片
范围分片是按照 key 的范围划分数据。例如：
- `user_id` 1 到 100 万放在分片 A。
- `user_id` 100 万到 200 万放在分片 B。
- `user_id` 200 万到 300 万放在分片 C。

优点是范围查询方便，数据有序，适合按时间、ID、区间查询。

缺点是容易产生热点。例如按照时间分片，最新数据都写入当前月份或当前日期的分片，导致新分片压力特别大，老分片几乎没有写入。

在 Java 业务中，日志表、流水表、归档表常见按时间范围拆分，例如按月表：`order_2026_01`、`order_2026_02`。这种方式便于归档和清理，但如果当前月流量特别大，仍然可能形成热点。

#### 5. 基于哈希分片
哈希分片是对分片键做哈希，再根据哈希值决定数据落在哪个分片。

例如：

```java
int shard = hash(userId) % 16;
```

这样可以把数据比较均匀地分散到多个分片上。

优点是数据分布更均匀，不容易因为连续 ID 或时间顺序造成热点。

缺点是范围查询不方便，因为相邻的数据会被打散到不同分片中。比如想查某个时间范围内的所有订单，就可能需要扫描多个分片。

MySQL 分库分表中很常见的做法是按用户 ID 或订单 ID 取模分片，例如：

```java
tableIndex = userId % 32;
```

这种方式简单直接，但扩容时比较麻烦。如果从 32 张表扩到 64 张表，原来的取模规则变化，很多数据都需要迁移。

#### 6. Redis 哈希槽
Redis Cluster 没有直接把 key 映射到节点，而是先把 key 映射到 16384 个哈希槽，再把槽分配给不同节点。

大致过程是：
- 对 key 计算 CRC16。
- 对 16384 取模，得到槽位。
- 根据槽位找到负责该槽的 Redis 节点。

这种设计的好处是节点和 key 之间多了一层槽位映射，扩容和缩容时只需要移动一部分槽，而不是重新计算所有 key 的节点归属。

例如原来 3 个节点分别负责一部分槽，新增第 4 个节点时，可以从原来的节点迁移部分槽给新节点。客户端只需要知道槽到节点的映射关系。

Redis 哈希槽给我的启发是：分片系统最好不要让业务代码直接绑定物理节点，而应该引入一层逻辑分片或路由层，方便后续扩容和迁移。

#### 7. 分片再平衡
再平衡指的是当节点数量变化或数据分布不均时，重新调整数据在分片之间的分布。

例如：
- 新增机器，需要把一部分数据迁移过去。
- 某个分片数据太多，需要拆分。
- 某个节点故障，需要把它的数据迁移或恢复到其他节点。
- 某些热点数据集中，需要重新规划分片。

再平衡的难点在于：迁移过程中系统还要继续提供读写服务。需要处理数据复制、增量同步、路由切换、双写、校验、回滚等问题。

在 MySQL 分库分表中，如果早期使用 `user_id % 16`，后续要扩成 `% 32`，迁移成本会很高，因为大量数据的分片位置都会变化。相比之下，一致性哈希、虚拟节点、逻辑分片、Redis 哈希槽等设计都在试图降低再平衡成本。

#### 8. 热点问题
分片的目标是把负载分散出去，但如果分片键选择不好，仍然可能产生热点。

常见热点包括：
- 某个大客户的数据量远超普通用户。
- 某个爆款商品被大量访问。
- 某个明星用户的动态被频繁读取。
- 按时间分片时，最新时间段承载绝大多数写入。
- 某个 Redis hot key 被大量访问。

例如按 `user_id` 分片时，如果某个企业客户下面有海量订单，这个用户所在分片会特别大。按商品 ID 分片时，爆款商品可能让某个分片压力暴涨。

解决热点问题的思路包括：
- 选择更均匀的分片键。
- 对热点 key 做打散。
- 对大客户做单独分片。
- 加缓存或本地缓存降低读压力。
- 对写热点引入队列削峰。
- 对时间分片再叠加哈希分片。

#### 9. 跨分片查询
分片后，最大的开发复杂度之一是跨分片查询。

如果查询条件包含分片键，就可以直接路由到一个分片。例如按 `user_id` 分片时，查询某个用户的订单列表很简单。

但如果查询条件不包含分片键，就可能需要查询所有分片再聚合。例如按 `user_id` 分片后，运营后台按 `order_no`、`phone`、`status` 或时间范围查订单，就可能变得复杂。

常见解决方式包括：
- 建立全局二级索引。
- 冗余查询维度表。
- 使用搜索引擎承接复杂查询。
- 后台低频查询允许扫多个分片。
- 设计时明确核心查询必须带分片键。

这也是为什么分库分表前一定要梳理查询场景。分片不是只影响写入，也会改变所有查询方式。

#### 10. 跨分片事务
单库单表中，一个事务可以很自然地更新多行数据。但分片后，如果一个业务操作涉及多个分片，就会出现跨分片事务问题。

例如一个转账操作：
- 用户 A 的账户在分片 1。
- 用户 B 的账户在分片 2。
- 一次转账需要同时扣 A、加 B。

如果用传统本地事务，就无法覆盖两个分片。此时可能需要分布式事务、最终一致性、TCC、Saga、可靠消息、补偿任务等方案。

但这些方案都会增加复杂度。所以在分片设计时，应该尽量让一个核心业务操作落在同一个分片内。例如按用户维度组织用户相关数据，减少跨用户、跨分片强事务。

对 Java 开发来说，分库分表后最大的变化之一就是：不能再默认数据库事务可以覆盖所有数据。

#### 11. 二级索引与全局索引
在单机数据库中，二级索引可以帮助通过非主键字段查询数据。但分片后，本地二级索引只能在单个分片内生效。

如果不知道数据在哪个分片，只靠本地索引不够。例如订单按 `user_id` 分片，但现在要根据 `order_no` 查询订单。除非 `order_no` 本身能反推出分片，否则就需要额外索引。

常见做法包括：
- 让订单号中包含分片信息。
- 建一张 `order_no -> user_id` 的映射表。
- 建全局索引表。
- 把订单同步到 Elasticsearch，供后台查询。
- 查询所有分片后合并结果，但只适合低频场景。

这说明分片会让原来简单的查询变成系统设计问题。

#### 12. 分片与业务建模
分片键往往会反过来影响业务建模。

例如订单系统如果按 `user_id` 分片，就很适合用户视角查询订单，但运营视角、商家视角、支付视角查询可能需要其他索引或数据冗余。

如果按 `merchant_id` 分片，商家后台查询方便，但用户订单列表可能需要跨分片。

如果按 `order_id` 分片，订单详情查询方便，但用户维度和商家维度都需要辅助索引。

所以分片不是数据库层独立决策，而是业务访问路径的体现。分片键选择不好，后面大量业务代码都会被迫补偿。

### 架构中的主要权衡

#### 容量扩展 vs 查询复杂度
分片可以突破单机容量限制，但会让查询变复杂。查询包含分片键时很高效，不包含分片键时可能需要全分片扫描、全局索引或异步同步到搜索系统。

#### 数据均匀 vs 范围查询
哈希分片可以让数据分布更均匀，但范围查询困难。范围分片支持区间查询和归档，但容易出现热点。选择时要看业务更看重均匀写入，还是范围扫描。

#### 简单取模 vs 扩容成本
取模分片实现简单，业务容易理解，但扩容时会导致大量数据重新分布。引入逻辑分片、一致性哈希或哈希槽会增加设计复杂度，但能降低后续迁移成本。

#### 单分片事务 vs 跨分片事务
把相关数据放在同一个分片，可以继续使用本地事务，开发简单。但如果业务天然跨多个主体，就可能需要分布式事务或最终一致性方案。分片设计要尽量减少核心链路上的跨分片事务。

#### 早期简单架构 vs 后期迁移成本
不分片时开发简单，但数据量增长后可能面临大规模迁移。过早分片又会增加不必要的复杂度。更好的做法是早期在模型和 ID 设计上预留分片可能性，但不要在业务还没验证前过度拆分。

### 对日常 Java 开发的实际帮助

#### 1. 更理解水平拆分的代价
以前可能只觉得表大了就分表，但分表之后，分页、排序、聚合、事务、唯一约束、后台查询都会变复杂。分片解决的是容量和吞吐问题，不是免费优化。

#### 2. 更重视分片键设计
分片键应该围绕核心查询和核心写入设计。比如 C 端订单系统通常优先考虑用户维度，B 端订单系统可能更关注商家维度。分片键一旦选错，后续改动成本非常高。

#### 3. 更能理解 Redis Cluster
Redis 的哈希槽本质上是一种逻辑分片层。它不是简单 `key % nodeCount`，而是通过 16384 个槽降低节点扩容和迁移成本。理解哈希槽后，也更容易理解为什么有些多 key 操作要求 key 落在同一个 slot。

#### 4. 更谨慎使用跨分片查询
分库分表后，如果后台查询随便组合条件，就可能变成扫所有分片。业务设计时应该区分在线高频查询和后台低频查询，高频查询必须有明确路由路径。

#### 5. 更关注全局唯一 ID
分片系统通常不能再依赖单库自增 ID 作为全局主键。需要使用雪花算法、号段模式、UUID、数据库发号器等方式生成全局唯一 ID。

同时，ID 设计还可以携带时间、业务类型或分片信息，帮助路由和查询。

#### 6. 更理解为什么要做数据冗余
分片后，为了支持不同维度查询，常常需要冗余数据。例如订单主表按用户分片，但商家后台需要按商家查订单，就可能需要商家订单索引表或同步到搜索引擎。

这类冗余不是脏设计，而是在分布式数据系统中用空间和同步复杂度换查询效率。

#### 7. 更谨慎引入分库分表中间件
ShardingSphere、MyCAT 或自研路由层可以屏蔽一部分分片复杂度，但不能消除分片本身的约束。开发仍然要知道哪些查询会广播、哪些事务会跨库、哪些排序分页会变慢。

中间件能降低接入成本，但不能替业务做正确的数据建模。

### 实践举例

#### 1. 订单表按用户 ID 分片
如果订单系统主要面向用户查询，例如用户查看自己的订单列表，可以按 `user_id` 分片。

优点：
- 用户订单列表可以直接路由到单个分片。
- 用户维度的数据比较集中。
- 大部分 C 端查询性能稳定。

缺点：
- 按订单号查询需要辅助映射。
- 商家后台查询可能需要额外索引。
- 大客户或高活跃用户可能造成热点。

适合场景：用户维度查询是核心链路，运营和商家查询可以通过冗余索引或搜索系统解决。

#### 2. 订单表按订单 ID 分片
如果订单详情查询非常核心，或者订单号本身可以编码分片信息，可以按 `order_id` 分片。

优点：
- 根据订单号查详情很方便。
- 数据分布通常比较均匀。
- 不容易因为少数用户产生严重热点。

缺点：
- 用户订单列表需要额外索引。
- 商家订单列表也需要额外索引。
- 和用户相关的事务可能跨分片。

适合场景：订单号是最主要查询入口，其他维度通过索引表或搜索系统补充。

#### 3. 日志表按时间分片
日志、流水、操作记录常常按时间分片，例如按天表或按月表。

优点：
- 归档和清理方便。
- 按时间范围查询自然。
- 历史数据可以迁移到低成本存储。

缺点：
- 最新时间分片容易成为写热点。
- 如果当前分片数据量太大，还需要叠加哈希分片。
- 跨月查询需要合并多张表。

适合场景：数据天然按时间增长，历史数据访问频率低。

#### 4. Redis Cluster 的 key 设计
Redis Cluster 中，不同 key 可能落到不同 slot。如果要做多 key 操作，例如 `MGET` 或 Lua 脚本，通常要求这些 key 在同一个 slot。

可以通过 hash tag 控制槽位，例如：

```text
cart:{userId}:items
cart:{userId}:summary
```

这样两个 key 会根据 `{userId}` 计算 slot，从而落到同一个槽。

这个例子说明，即使是 Redis，分片也会影响 key 的命名方式和业务操作设计。

#### 5. 后台查询与搜索引擎
订单主链路按用户分片后，运营后台可能需要按手机号、订单状态、商品名称、时间范围组合查询。如果这些查询直接打到分片库，可能需要广播到所有分片。

更常见的做法是：
- 主库承接核心写入和单用户查询。
- 通过 binlog 或消息同步到 Elasticsearch。
- 后台复杂查询走搜索引擎。
- 查询结果再回源订单库获取详情。

这说明分片系统里，不同查询场景可能需要不同的数据视图。

### 我的理解
- 分片的核心是把数据和负载拆开，让系统突破单机容量和吞吐限制。
- MySQL 水平拆分和 Redis 哈希槽本质上都是分片，只是一个更多由业务或中间件管理，一个由 Redis Cluster 管理。
- 分片键是分片设计中最重要的决策，它决定了数据分布、查询路径、热点风险和后续扩容成本。
- 哈希分片解决均匀性问题，但牺牲范围查询；范围分片方便区间查询和归档，但容易产生热点。
- 分库分表后，很多单库时代简单的问题都会变复杂，例如分页、排序、聚合、唯一约束、二级索引和事务。
- 分片不是越早做越好，过早分片会增加开发和运维复杂度；但完全不预留分片能力，后期迁移也会很痛苦。
- 后端开发不能只把分片看作 DBA 或中间件问题，因为业务查询方式、ID 设计和事务边界都会被分片影响。

### 可落地的实践启发
- 分片前先梳理核心查询路径，不要只看表数据量。
- 分片键优先选择高频查询必带、分布相对均匀、业务稳定的字段。
- 不要默认所有后台查询都能在分片库上高效完成，复杂查询可以交给搜索引擎或数仓。
- 订单、流水等大表要提前考虑全局唯一 ID 和分片路由能力。
- 取模分片实现简单，但要提前评估未来扩容迁移成本。
- 对时间分片要警惕最新分片写入热点，必要时叠加哈希拆分。
- 对 Redis Cluster 要理解 slot 和 hash tag，避免多 key 操作跨 slot。
- 分库分表后，尽量让核心事务落在同一个分片内。
- 对跨分片事务优先考虑业务拆分、最终一致性和补偿，而不是盲目上分布式事务。
- 对热点用户、热点商品、热点 key 要有单独治理策略。
- 引入分片中间件前，要确认团队理解广播查询、分布式事务、分页排序和扩容迁移的限制。

### 一句话总结
分片的核心不是简单地把数据拆到多台机器上，而是围绕业务访问模式设计数据分布方式；好的分片设计能提升容量和吞吐，差的分片设计会把查询、事务、扩容和热点问题全部留给后续业务系统承担。
