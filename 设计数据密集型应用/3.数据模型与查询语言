# 《设计数据密集型应用》读书笔记

## 第 3 章：数据模型与查询语言

### 本章核心
这一章讲的是：数据应该如何组织，以及不同组织方式会如何影响查询、开发、扩展和维护。作为 Java 后端工程师，不一定马上要使用文档数据库、图数据库或其他查询语言，但需要理解不同数据模型背后的取舍，避免所有问题都只用 MySQL 或 Redis 来解决。

### 我读完后的直观感受
这一章容易读得云里雾里，因为它不是在教某个数据库的具体用法，而是在抽象地讲各种数据模型。对日常开发来说，最重要的不是记住所有模型细节，而是建立一种意识：**数据模型会决定系统未来怎么查、怎么改、怎么扩展。**

### 关键概念

#### 1. 关系模型
关系模型就是平时最熟悉的 MySQL、PostgreSQL 这类数据库模型，核心是表、行、列、主键、外键和 SQL。

它的优点：
- 结构清晰，适合表达业务实体。
- 支持事务，适合订单、支付、库存等强一致场景。
- SQL 查询能力强，可以 join、过滤、排序、聚合。
- 数据规范化后，冗余较少，更新一致性更容易保证。

它的问题：
- 多表 join 在数据量大或查询复杂时可能变慢。
- 对层级结构、嵌套结构表达不够自然。
- 表结构变更需要谨慎，尤其是大表加字段、改字段。
- 高并发读写场景下，需要索引、分库分表、缓存等配合。

对 Java 开发的意义：
- 平时写实体类、Mapper、SQL，本质上都是在围绕关系模型工作。
- 设计表结构时，要考虑业务关系、查询方式、索引和事务边界。
- 不要只想着“能存进去”，还要想“未来怎么查、怎么改、数据量大了怎么办”。

#### 2. 文档模型
文档模型可以理解为把一组相关数据放在一个 JSON 文档里，例如 MongoDB。它更接近 Java 里的对象结构。

适合场景：
- 一次读取一整个聚合对象。
- 数据结构有一定变化，不想频繁改表。
- 数据之间关系不复杂，主要围绕单个主体查询。

例子：
一个订单详情如果经常整体展示，可以把订单、收货地址、商品快照、优惠信息放在一个文档里，一次查出完整内容。

优点：
- 读取完整对象方便。
- 嵌套结构表达自然。
- 某些场景下可以减少 join。

问题：
- 数据容易冗余。
- 局部更新和一致性需要额外考虑。
- 如果后期需要跨文档复杂查询，会比较困难。

对 Java 开发的意义：
- 即使用 MySQL，也可能会用 JSON 字段、快照字段、冗余字段，这些思想和文档模型类似。
- 在接口返回、缓存设计、ES 索引设计中，也经常会把数据组织成文档形式。

#### 3. 键值模型
键值模型最典型的就是 Redis。核心思想是通过 key 快速找到 value。

适合场景：
- 缓存热点数据。
- Session、Token、验证码。
- 计数器、排行榜、限流。
- 分布式锁。
- 临时状态数据。

优点：
- 访问速度快。
- 数据结构简单。
- 很适合按 key 精确读取。

问题：
- 不适合复杂条件查询。
- 不适合作为核心业务数据的唯一存储。
- key 设计不好会导致维护困难。
- 需要考虑过期时间、缓存一致性、缓存穿透、击穿和雪崩。

对 Java 开发的意义：
- Redis 不是 MySQL 的替代品，而是围绕访问性能和临时状态做补充。
- 设计 Redis key 时，其实也是在做数据建模。
- 需要根据查询方式设计 key，而不是随便把对象序列化进去。

#### 4. 图模型
图模型适合表达复杂关系，核心是节点和边。比如用户是节点，好友关系、关注关系、权限继承关系是边。

适合场景：
- 社交网络关系。
- 推荐系统。
- 知识图谱。
- 权限继承和组织关系。
- 路径查找，比如 A 和 B 之间有什么关系。

优点：
- 表达多对多和深层关系很自然。
- 适合关系遍历和路径查询。

问题：
- 日常业务系统不一定需要。
- 团队学习和运维成本较高。
- 如果只是简单父子关系或少量关联，用 MySQL 就够了。

对 Java 开发的意义：
- 不一定要马上学图数据库，但要知道某些复杂关系问题用关系表硬查会很痛苦。
- 当业务开始出现大量“关系路径”“多层推荐”“权限继承”时，可以想到图模型是一种可能方案。

#### 5. 查询语言
SQL 是声明式查询语言。我们只描述“我要什么数据”，数据库负责决定怎么执行。

声明式查询的好处：
- 开发者不需要手写遍历过程。
- 数据库可以自动优化执行计划。
- 查询逻辑相对简洁。

与之相对的是命令式查询：你要一步步告诉程序怎么查、怎么遍历、怎么过滤。

对 Java 开发的意义：
- 写 SQL 时，不只是写字符串，而是在描述数据需求。
- 数据库执行 SQL 时，会结合索引、统计信息和执行计划选择查询方式。
- 遇到慢 SQL 时，不能只看代码，还要看表结构、索引、执行计划和数据分布。

### 和 MySQL、Redis 的对应关系

#### MySQL
- 属于关系模型。
- 适合核心业务数据、事务、一致性要求高的数据。
- 常见问题是表设计、索引设计、慢 SQL、事务范围、锁竞争和分库分表。

#### Redis
- 属于键值模型，也提供 list、hash、set、zset 等结构。
- 适合缓存、临时状态、高频访问和简单数据结构操作。
- 常见问题是缓存一致性、key 设计、内存占用、过期策略和热点 key。

#### Elasticsearch
虽然不是本章重点，但可以理解为面向搜索和文档查询的系统。
- 适合全文检索、复杂筛选、日志查询。
- 不适合作为强一致的核心业务库。

#### MongoDB
更接近文档模型。
- 适合结构灵活、以聚合对象为中心的查询。
- 如果业务强依赖复杂关联和事务，关系数据库通常更合适。

### 架构中的主要权衡

#### 规范化 vs 反规范化
规范化可以减少数据冗余，保证更新一致性；反规范化可以减少查询 join，提高读取性能。实际开发中经常两者结合：核心数据保持规范化，查询频繁的字段适当冗余。

#### 灵活查询 vs 快速读取
关系数据库适合灵活查询，但复杂查询可能有性能压力。文档模型和缓存模型适合快速读取固定视图，但不适合任意条件组合查询。

#### 一致性 vs 性能
数据冗余、缓存、异步同步都能提升性能，但会带来一致性问题。需要明确哪些数据必须立即一致，哪些可以接受短暂延迟。

#### 通用能力 vs 专用能力
MySQL 很通用，但全文搜索不如 Elasticsearch，复杂关系遍历不如图数据库，高速缓存不如 Redis。专用系统能解决特定问题，但会增加系统复杂度和运维成本。

### 对日常开发的实际帮助

#### 1. 设计表结构时更有意识
不只是按页面字段建表，而是考虑实体关系、查询路径、更新频率、数据量增长和索引设计。

#### 2. 写接口时更关注访问模式
先想清楚接口是按主键查、按条件查、聚合统计、全文搜索，还是关系遍历。不同访问模式适合不同数据组织方式。

#### 3. 使用 Redis 时更重视 key 设计
Redis 的 key 就是查询入口。key 设计不好，后面会出现难维护、难批量处理、难定位问题。

#### 4. 遇到复杂查询时多一种思路
不是所有问题都靠 MySQL join 解决。搜索问题可以考虑 ES，热点读取可以考虑缓存，复杂关系可以考虑图模型，报表分析可以考虑专门的分析库。

#### 5. 更容易理解架构选型
当项目里出现 MySQL、Redis、ES、MongoDB、ClickHouse 等组件时，能理解它们分别解决什么问题，而不是觉得只是“多引入了几个数据库”。

### 我的理解
- 数据模型本质上是在回答：业务数据之间是什么关系，以及我们打算怎样访问这些数据。
- MySQL 适合做核心事实数据的可靠存储，Redis 适合做高性能辅助访问，不应该混淆它们的职责。
- 文档模型、图模型不一定天天用，但理解它们能帮助我在遇到特殊业务场景时不被 MySQL 思维限制。
- 数据建模不是 DBA 的事情，后端开发写实体、SQL、缓存、接口返回结构时，其实都在参与数据建模。
- 一个好的数据模型，应该同时服务于业务表达、查询效率、数据一致性和后期维护。

### 可落地的实践启发
- 建表前先列出主要查询场景，而不是只列字段。
- 核心业务数据优先保证一致性和可维护性，不要过早为了性能大量冗余。
- 对高频读取、低频变化的数据，可以考虑 Redis 缓存或冗余视图。
- 对需要搜索的字段，不要硬用 like 扛所有需求，可以考虑搜索引擎。
- 对 Redis key 做统一命名规范，例如 `业务:对象:标识:属性`。
- 慢 SQL 排查时同时看 SQL、索引、执行计划、数据量和数据分布。
- 当表关系越来越复杂时，重新审视是建模问题、查询问题，还是选型问题。

### 暂时可以不深入的内容
- Datalog。
- RDF 和 SPARQL。
- 三元组存储。
- 复杂图查询语言。
- 学术型查询模型。

这些内容在知识图谱、语义网、复杂关系推理等领域更常见。作为 Java 后端工程师，先理解它们解决的是“复杂关系表达和查询”问题即可，不需要一开始就掌握细节。

### 一句话总结
第三章真正想传达的是：数据模型会深刻影响系统的查询方式、代码结构、性能表现和扩展能力；作为 Java 后端，不必马上掌握所有数据库模型，但要知道不同模型适合解决不同类型的问题。
