# 《设计数据密集型应用》读书笔记

## 第 13 章：流式系统的哲学

### 本章核心
这一章讲的是流式系统背后的思想：不要总是把数据系统理解成“数据库里保存当前状态，然后通过事务一次性改完”，也可以把系统理解成“一连串持续发生的事件”。状态不是唯一事实来源，事件流才是事实的历史记录，当前状态只是从事件中派生出来的一个视图。

对 Java 后端开发来说，这一章最大的冲击是：传统业务开发通常依赖数据库事务来保证一致性，而流式系统提供了另一种思路：用事件驱动、异步处理、幂等消费、状态机、补偿和最终一致性，来替代一部分刚性事务。

这不是说事务不重要，而是说在分布式系统和跨服务协作中，很多事情不一定要放在一个强事务里一次性完成。可以把业务拆成多个事件驱动的步骤，让系统通过事件逐步收敛到正确状态。

### 我读完后的直观感受
这一章给我引入了一种在 Java 日常开发中不算常见，但非常重要的模型：事件驱动模型。

以前写 Java 业务时，我更多想到的是：
- 一个 `Service` 方法里开启 `@Transactional`。
- 在事务里完成多张表写入。
- 调用其他服务或 MQ 完成后续动作。
- 如果失败就回滚。

但这一章让我意识到，系统也可以换一种方式设计：
- 用户操作产生一个事件。
- 事件被可靠记录下来。
- 多个消费者订阅事件。
- 每个消费者只负责自己的状态变更。
- 如果失败，就重试、补偿或人工处理。
- 最终各个系统状态达成一致。

Java 里其实也有观察者模式、事件发布机制、Spring ApplicationEvent、领域事件、MQ 消息、监听器等概念。但以前更多把它们当成“解耦小工具”，很少把它们上升到“替代部分事务边界”的架构思想。

这一章真正让我思考的是：很多跨服务事务不一定要追求刚性强一致，而可以通过事件驱动实现柔性事务。

### 关键概念

#### 1. 数据库保存的是当前状态，事件保存的是变化历史
传统数据库表通常保存当前状态。

例如订单表中有一行：

```text
order_id = 1001, status = PAID
```

它告诉我们订单当前是已支付，但它没有完整表达这个状态是怎么来的。

事件流则保存变化历史：

```text
OrderCreated
PaymentStarted
PaymentSucceeded
OrderPaid
InventoryDeducted
CouponUsed
```

通过这些事件，我们可以知道订单从创建到支付成功的完整过程。

当前状态可以从事件中派生出来。例如订单当前状态是对事件流重放后的结果。

这带来的思想变化是：
- 表是状态视图。
- 事件是事实记录。
- 状态可以重建。
- 历史可以追溯。
- 不同系统可以基于同一批事件构建不同视图。

#### 2. 事件是不可变事实
事件一旦发生，就不应该被随意修改。

例如 `PaymentSucceeded` 表示支付成功这个事实已经发生。后续如果发现支付异常，不应该修改原事件，而应该追加一个新的事件，例如 `PaymentReversed` 或 `RefundCreated`。

这和普通数据库更新很不一样。传统状态表可能是：

```sql
update orders set status = 'REFUNDED' where id = 1001;
```

事件模型更像是追加：

```text
OrderPaid
RefundRequested
RefundSucceeded
OrderRefunded
```

这种方式更适合审计、追踪、对账和问题排查，因为系统保留了完整历史。

对 Java 业务来说，这一点很有价值。很多线上问题之所以难查，是因为我们只看到最终状态，却看不到状态变化过程。如果关键业务都能沉淀事件和流水，排查会容易很多。

#### 3. 事件驱动与观察者模式
Java 中的观察者模式是事件驱动的基础思想之一。

一个对象状态变化后，通知多个观察者执行后续逻辑。

例如：

```java
orderService.createOrder(command);
eventPublisher.publish(new OrderCreatedEvent(orderId));
```

然后不同监听器处理不同事情：

```java
public void onOrderCreated(OrderCreatedEvent event) {
    inventoryService.reserve(event.orderId());
}

public void onOrderCreated(OrderCreatedEvent event) {
    notificationService.sendCreateMessage(event.orderId());
}
```

但普通观察者模式通常在单进程内运行，可靠性有限。如果进程崩溃，事件可能丢失；如果监听器失败，可能没有重试。

真正用于业务一致性的事件驱动，需要更可靠的事件机制，例如：
- 本地消息表。
- MQ。
- Kafka。
- 事务事件。
- Outbox 模式。
- CDC/binlog 订阅。

所以 Java 中的观察者模式只是思想入口，生产级事件驱动还要解决可靠投递、幂等消费、失败重试和状态补偿。

#### 4. 事件驱动不是不要事务
事件驱动不是完全抛弃事务，而是缩小事务边界。

例如创建订单时，仍然需要本地事务保证订单主表、订单明细、订单事件记录同时写入成功。

```java
@Transactional(rollbackFor = Exception.class)
public void createOrder(CreateOrderCommand command) {
    orderRepository.save(order);
    orderItemRepository.saveItems(items);
    eventRepository.save(new OrderCreatedEvent(order.getId()));
}
```

这里的本地事务仍然很重要。它保证“订单创建”和“订单创建事件记录”不会一个成功一个失败。

但库存扣减、发优惠券、发送通知、同步搜索索引等后续动作，不一定要放在同一个大事务里，而是可以由事件异步触发。

也就是说：
- 本地核心状态用事务保证。
- 跨服务后续动作通过事件实现最终一致。
- 事件驱动替代的是跨系统刚性事务，不是替代所有事务。

#### 5. 刚性事务与柔性事务
刚性事务追求的是强一致：多个操作要么全部成功，要么全部失败，通常依赖数据库本地事务、XA、2PC 等机制。

柔性事务追求的是最终一致：允许系统短时间内处于中间状态，但通过事件、重试、补偿和对账，最终达到业务可接受的一致状态。

例如下单流程中：
- 订单服务创建订单成功。
- 库存服务稍后扣减库存。
- 优惠券服务稍后核销优惠券。
- 通知服务稍后发送消息。

在短时间内，订单可能已经创建，但库存还没扣完。这不是立即强一致，但如果系统能保证后续事件最终处理成功，或者失败后订单被取消并释放资源，那么整体上就是柔性事务。

柔性事务适合：
- 跨服务操作。
- 可接受短暂中间状态。
- 可以重试或补偿。
- 业务过程天然有多个阶段。
- 不希望 2PC 阻塞整个链路。

不适合简单柔性事务的场景：
- 余额扣减必须立即准确。
- 强监管账务记账。
- 无法容忍中间状态。
- 无法补偿或补偿成本极高。

#### 6. 事件日志与数据库日志
流式系统强调日志。日志不是普通业务日志，而是一种有序、可追加、可重放的数据结构。

Kafka 的 topic、数据库 binlog、事件存储，都可以理解成事件日志。

事件日志的价值包括：
- 记录发生过的事实。
- 支持多个消费者独立订阅。
- 支持失败后从某个位置继续消费。
- 支持重放历史事件重建状态。
- 支持新增下游系统时补历史数据。

例如订单事件流可以被多个系统消费：
- 库存系统消费 `OrderCreated` 冻结库存。
- 积分系统消费 `OrderPaid` 发积分。
- 搜索系统消费订单事件更新索引。
- 风控系统消费订单事件分析风险。
- 报表系统消费订单事件生成指标。

这比在订单服务里同步调用所有下游更可扩展。

#### 7. 状态是事件流的物化视图
流式系统中，状态可以看作事件流计算出来的结果。

例如用户积分余额可以由积分流水事件累加得到：

```text
PointAdded +100
PointUsed -20
PointAdded +50
```

最终余额是 130。

订单状态也可以由事件流推导：

```text
OrderCreated -> WAIT_PAY
PaymentSucceeded -> PAID
RefundSucceeded -> REFUNDED
```

实际系统中，为了查询方便，仍然会保存状态表。但要理解状态表只是一个物化视图，背后最好有事件或流水作为事实依据。

这对 Java 开发很重要。很多时候状态表错了，如果没有事件或流水，就很难恢复；如果有完整事件流，就可以重新计算状态。

#### 8. CQRS
CQRS 是 Command Query Responsibility Segregation，也就是命令查询职责分离。

简单理解：
- Command 负责写入和业务决策。
- Query 负责查询和展示。

在事件驱动系统中，写模型可以产生事件，读模型可以订阅事件构建自己的查询视图。

例如订单服务写入订单事件，查询侧可以构建：
- 用户订单列表视图。
- 商家订单管理视图。
- 运营后台订单搜索视图。
- 报表统计视图。

这些读模型不一定都和写模型使用同一张表。它们可以由事件异步更新。

这对 Java 项目很有启发：复杂系统里，不要强迫一套表同时满足所有写入、查询、搜索和报表需求。可以让核心写模型保持简单，通过事件派生多个读视图。

#### 9. 事件溯源
事件溯源是把事件作为主要事实来源，而不是只保存当前状态。

普通状态模型是：
- 写入订单表。
- 更新订单状态。
- 最终只保存当前状态。

事件溯源模型是：
- 保存 `OrderCreated`。
- 保存 `PaymentSucceeded`。
- 保存 `OrderShipped`。
- 保存 `OrderCompleted`。
- 当前订单状态由事件重放得到。

事件溯源的优点：
- 完整历史可追踪。
- 状态可以重建。
- 便于审计和对账。
- 可以新增读模型并重放历史。

缺点：
- 开发复杂度高。
- 查询当前状态需要物化视图。
- 事件 schema 演化要谨慎。
- 重放逻辑和版本兼容比较复杂。

对普通 Java 项目来说，不一定要完整采用事件溯源，但可以借鉴它的思想：关键业务一定要有流水、事件和状态变更记录。

#### 10. Outbox 模式
Outbox 模式是 Java 项目中非常实用的事件驱动落地方式。

问题是：业务写数据库和发送 MQ 不是一个原子操作。

如果先写数据库，再发 MQ：
- 数据库成功。
- MQ 发送失败。
- 下游永远收不到事件。

如果先发 MQ，再写数据库：
- MQ 发送成功。
- 数据库写入失败。
- 下游收到一个实际上不存在的业务事件。

Outbox 模式的做法是：在同一个本地事务中写业务表和消息表。

```java
@Transactional(rollbackFor = Exception.class)
public void paySuccess(PaySuccessCommand command) {
    orderRepository.markPaid(command.orderId());
    outboxRepository.save(new OutboxEvent("OrderPaid", command.orderId()));
}
```

然后由后台任务或消息投递器扫描 outbox 表，把事件发送到 MQ。发送成功后标记消息已发送。

这样可以保证：只要业务状态提交了，对应事件也一定记录下来了。

Outbox 是用本地事务连接业务状态和事件流的关键模式，非常适合 Java 后端。

#### 11. CDC 与 binlog 订阅
另一种事件驱动方式是 CDC，也就是 Change Data Capture，捕获数据库变更。

例如通过 MySQL binlog 订阅订单表变化，然后把变化同步到 Kafka、Elasticsearch 或其他系统。

常见工具包括 Canal、Debezium、Flink CDC 等。

CDC 的优点是：
- 不需要业务代码显式发消息。
- 可以基于数据库提交后的变更生成事件。
- 适合数据同步、索引更新、报表链路。

缺点是：
- 业务语义可能不如领域事件清晰。
- 表结构变化会影响下游。
- 需要处理顺序、幂等、schema 演化。
- 数据库表变更不一定等同于业务事件。

例如订单表 `status` 从 `WAIT_PAY` 改为 `PAID`，CDC 能捕获字段变化，但业务上更清晰的事件应该是 `OrderPaid`。

所以领域事件和 CDC 各有适用场景：
- 领域事件适合表达业务事实。
- CDC 适合做数据同步和视图构建。

#### 12. 流式系统中的时间
流式系统会遇到时间问题。事件发生时间、处理时间、到达时间可能不同。

例如支付成功事件实际发生在 10:00，但因为网络延迟，10:05 才被消费者处理。

这会影响：
- 报表统计。
- 窗口计算。
- 超时判断。
- 事件顺序。
- 补偿逻辑。

Java 业务中也很常见。比如订单超时关闭和支付成功事件并发到达时，不能只按消费者处理时间决定订单状态，而要依赖状态机和业务规则。

#### 13. 事件乱序与重复
事件驱动系统中，消息可能乱序、重复、延迟。

例如订单相关事件可能按以下顺序到达消费者：

```text
OrderPaid
OrderCreated
```

或者同一个 `OrderPaid` 被投递两次。

所以消费者必须具备：
- 幂等处理能力。
- 状态机校验能力。
- 版本号或事件序号判断能力。
- 对缺失前置事件的等待或补偿能力。

不能假设 MQ 事件永远按业务顺序准确到达。

#### 14. 事件驱动下的错误处理
事件驱动系统不是没有错误，而是把错误从同步调用时的立即失败，变成异步处理中的重试、补偿和最终收敛。

常见错误处理方式包括：
- 消费失败后重试。
- 超过次数进入死信队列。
- 定时扫描失败事件补偿。
- 人工处理异常单。
- 对账任务校准状态。
- 消费者幂等保证重复安全。

这也是柔性事务的关键：不要求所有步骤同步一次成功，而是允许失败，然后通过可控机制修复。

### 架构中的主要权衡

#### 强一致 vs 最终一致
传统事务追求强一致，事件驱动更多追求最终一致。强一致理解简单，但跨服务成本高；最终一致性能和可用性更好，但需要接受中间状态，并设计补偿和对账。

#### 同步调用 vs 异步事件
同步调用流程直观，调用方能立即得到结果，但会增加服务耦合和链路延迟。异步事件能解耦系统，提高可扩展性，但调试、追踪和错误处理更复杂。

#### 当前状态 vs 历史事实
只保存当前状态查询方便，但历史丢失后难以追踪。保存事件历史更利于审计和重建，但存储和演化成本更高。

#### 单一模型 vs 多个视图
一套表同时服务写入、查询、搜索、报表，会越来越复杂。通过事件构建多个读视图更灵活，但要处理数据同步延迟和视图一致性。

#### 业务简单性 vs 架构复杂度
事件驱动很强大，但不是所有系统都需要。简单 CRUD 系统直接事务就够了。只有当跨服务、异步流程、补偿、审计、多读模型需求明显时，事件驱动才更有价值。

### 对日常 Java 开发的实际帮助

#### 1. 不要把所有动作塞进一个事务
创建订单后，发送短信、发积分、同步 ES、写报表，不应该全部放在一个 `@Transactional` 方法里。

更好的做法是：
- 事务内只完成核心业务状态。
- 同时记录领域事件或 outbox 消息。
- 事务提交后异步处理非核心动作。

这样可以减少长事务、降低耦合，也更容易处理失败。

#### 2. 用事件表达业务事实
事件不是简单的“数据变更通知”，而应该表达业务事实。

例如：
- `OrderCreated`
- `OrderPaid`
- `InventoryReserved`
- `CouponUsed`
- `RefundSucceeded`

比起“订单表更新了”这种技术事件，业务事件更容易被下游理解。

#### 3. 消费者必须幂等
事件驱动系统默认消息可能重复。Java 消费者不能假设一条消息只消费一次。

常见做法：
- 消息 ID 去重。
- 业务单号唯一索引。
- 消费记录表。
- 状态机条件更新。
- 版本号控制。

#### 4. 用状态机管理中间状态
柔性事务一定会产生中间状态，例如：
- `CREATED`
- `PAYING`
- `PAID`
- `INVENTORY_RESERVED`
- `CANCELLED`
- `FAILED`

状态机要明确哪些状态可以转移，哪些不可以。

例如订单已支付后，超时关单事件不能再把它关闭。

```sql
update orders
set status = 'CLOSED'
where id = ?
  and status = 'WAIT_PAY';
```

这种状态条件更新是事件驱动系统里非常重要的保护。

#### 5. 事件发布要可靠
不要简单地在事务里直接发 MQ，然后以为万事大吉。

更可靠的方式：
- 本地事务写业务表和 outbox 表。
- 后台任务投递 MQ。
- MQ 消费者幂等处理。
- 失败进入重试或死信。
- 定时任务扫描未发送消息。

这样才能把事件驱动真正用于业务一致性。

#### 6. 用对账和补偿兜底
事件驱动不是“发了消息就一定成功”。一定要有兜底机制。

例如订单支付成功后发积分：
- 实时事件负责及时发积分。
- 消费失败可以重试。
- 死信消息可以人工处理。
- 每日对账任务扫描已支付但未发积分订单。

柔性事务真正可靠，不是因为异步消息永远成功，而是因为系统允许失败并能修复。

### Java 中可落地的例子

#### 1. 下单后异步扣库存
传统刚性事务思路可能是：订单服务、库存服务、优惠券服务一起放进分布式事务。

事件驱动思路可以是：

1. 订单服务本地事务创建订单，状态为 `WAIT_INVENTORY`。
2. 同一事务写入 `OrderCreated` 事件到 outbox 表。
3. 消息投递器发送 `OrderCreated` 到 MQ。
4. 库存服务消费事件，冻结库存。
5. 库存冻结成功后发布 `InventoryReserved`。
6. 订单服务消费 `InventoryReserved`，把订单推进到 `WAIT_PAY`。
7. 如果库存冻结失败，库存服务发布 `InventoryReserveFailed`。
8. 订单服务消费失败事件，把订单改为 `CANCELLED`。

这个流程不是一个强事务，但它通过事件和状态机实现了最终一致。

关键设计点：
- 订单状态要允许中间态。
- 库存冻结要幂等。
- 事件消费要幂等。
- 失败事件要能驱动补偿。
- 长时间未收到库存结果的订单要有超时补偿任务。

#### 2. 支付成功后发积分
支付成功后给用户发积分，不应该和支付状态更新放在一个分布式事务里。

更好的设计：

1. 支付回调进入订单服务。
2. 本地事务中把订单状态从 `WAIT_PAY` 改成 `PAID`。
3. 同一事务写入 `OrderPaid` 事件。
4. 积分服务消费 `OrderPaid`。
5. 积分服务写积分流水，唯一键为 `order_id + point_type`。
6. 消费失败时 MQ 重试。
7. 每日批处理扫描已支付但未发积分的订单补偿。

这样即使积分服务短暂不可用，也不影响支付状态落库。

这体现了柔性事务：支付成功是核心事实，积分发放可以最终一致。

#### 3. 订单状态同步到 Elasticsearch
订单创建、支付、发货、完成后，需要同步到 ES 供后台搜索。

不要在订单事务里同步调用 ES，因为 ES 失败不应该导致订单事务回滚。

更好的设计：

1. 订单服务产生领域事件。
2. 同步服务消费订单事件。
3. 根据事件更新 ES 文档。
4. ES 更新使用订单 ID 做幂等覆盖。
5. 如果消费失败，重试或进入死信。
6. 定期批处理从 MySQL 重建或校验 ES 索引。

这里 ES 是订单状态的读模型，不是核心事实来源。核心事实仍然在订单数据库和订单事件中。

#### 4. 用户注册后的多个后置动作
用户注册成功后，可能需要：
- 发送欢迎短信。
- 创建默认账户。
- 发放新人优惠券。
- 初始化用户画像。
- 同步 CRM 系统。

如果这些都放在注册接口里同步执行，接口会很慢，而且任何一个下游失败都可能影响注册。

事件驱动设计：

1. 用户服务本地事务创建用户。
2. 写入 `UserRegistered` 事件。
3. 短信服务消费事件发短信。
4. 优惠券服务消费事件发券。
5. 画像服务消费事件初始化画像。
6. CRM 同步服务消费事件同步外部系统。

每个消费者独立失败、独立重试。用户注册这个核心动作不被非核心后置动作拖垮。

#### 5. 退款流程的柔性事务
退款通常涉及订单、支付渠道、账户、通知等多个系统。

事件驱动设计可以是：

1. 用户申请退款，订单服务创建退款单，状态 `REFUNDING`。
2. 写入 `RefundRequested` 事件。
3. 支付服务消费事件，调用渠道退款。
4. 渠道返回成功后，支付服务发布 `RefundSucceeded`。
5. 订单服务消费成功事件，订单状态改为 `REFUNDED`。
6. 通知服务消费事件，通知用户退款成功。
7. 如果渠道超时，退款单保持 `REFUNDING`，由定时任务查询结果。
8. 如果渠道失败，发布 `RefundFailed`，订单服务更新退款状态。

这个例子说明，柔性事务不是没有一致性，而是通过状态机、事件和补偿把一致性拉长到一个过程里。

#### 6. 库存扣减的事件化设计
库存是比较敏感的业务，不一定所有场景都适合完全异步。

可以分场景：
- 普通商品下单，可以先冻结库存，再支付成功后确认扣减。
- 秒杀商品高并发，可以先在 Redis 或库存服务中快速预扣，再异步落库。
- 账务类库存或强约束库存，需要更强事务或单库存服务串行化处理。

事件驱动不是盲目异步，而是根据业务一致性要求选择同步确认点。

例如：
- 下单前必须同步确认是否有库存。
- 支付后扣减可以通过事件确认。
- 失败后通过事件释放冻结库存。

#### 7. Spring 中的领域事件落地
在 Java 项目中，可以从简单方式开始。

应用服务中：

```java
@Transactional(rollbackFor = Exception.class)
public void createOrder(CreateOrderCommand command) {
    Order order = Order.create(command);
    orderRepository.save(order);
    outboxRepository.save(DomainEvent.of("OrderCreated", order.getId()));
}
```

后台投递器：

```java
public void publishOutboxEvents() {
    List<OutboxEvent> events = outboxRepository.findUnpublished(100);
    for (OutboxEvent event : events) {
        mqProducer.send(event);
        outboxRepository.markPublished(event.getId());
    }
}
```

消费者：

```java
public void onOrderCreated(OrderCreatedEvent event) {
    if (consumeLogRepository.exists(event.getEventId())) {
        return;
    }
    inventoryService.reserve(event.getOrderId());
    consumeLogRepository.save(event.getEventId());
}
```

这不是完整代码，但表达了核心设计：本地事务记录事件，异步可靠投递，消费者幂等处理。

### 设计思路

#### 1. 先区分核心事实和派生动作
设计事件驱动前，先问：哪些动作必须在当前事务中完成？哪些动作只是后置派生？

例如订单支付：
- 核心事实：订单支付成功。
- 派生动作：发积分、发短信、同步 ES、更新报表。

核心事实用本地事务保证，派生动作通过事件异步处理。

#### 2. 事件命名要表达业务事实
不要把事件命名成 `OrderUpdated` 这种模糊事件。

更好的命名：
- `OrderCreated`
- `OrderPaid`
- `OrderCancelled`
- `InventoryReserved`
- `RefundSucceeded`

事件名应该让消费者一眼知道发生了什么业务事实。

#### 3. 事件内容要稳定
事件是跨系统契约，不能随意改。

事件中应该包含：
- eventId。
- eventType。
- aggregateId。
- occurredAt。
- version。
- traceId。
- 必要业务字段。

不要把整个 Entity 直接塞进事件。事件应该是稳定的业务契约，而不是数据库结构的镜像。

#### 4. 设计消费者时默认重复、乱序、延迟
消费者不要假设：
- 消息只来一次。
- 消息按顺序来。
- 消息马上来。
- 前置事件一定已经处理。

消费者应该通过幂等表、状态机、版本号、重试和补偿来处理这些情况。

#### 5. 每个事件都要有失败归宿
事件处理失败后不能只打印日志。

至少要有：
- 重试机制。
- 最大重试次数。
- 死信队列或失败表。
- 告警。
- 人工处理入口。
- 补偿任务。

否则事件驱动会变成“消息发出去了，但没人知道下游失败了”。

#### 6. 状态机是柔性事务的骨架
柔性事务中，状态机会比事务边界更重要。

例如订单状态流转要明确：

```text
WAIT_PAY -> PAID -> SHIPPED -> COMPLETED
WAIT_PAY -> CANCELLED
PAID -> REFUNDING -> REFUNDED
```

每次状态更新都要带前置状态条件，防止乱序事件覆盖正确状态。

#### 7. 用批处理兜底事件流
事件驱动系统一定要配批处理兜底。

例如：
- 扫描 outbox 中未发送事件。
- 扫描 MQ 死信消息。
- 扫描已支付但未发积分订单。
- 对账支付渠道与本地流水。
- 校验 ES 与 MySQL 数据差异。

这把第 11 章批处理和第 13 章流式系统连接起来了：实时事件负责快速传播，批处理负责最终校准。

#### 8. 不要过度事件化
事件驱动不是银弹。

不适合过度事件化的场景：
- 简单 CRUD。
- 单库单表事务能解决的问题。
- 业务规则很简单且没有下游订阅。
- 对实时一致性要求极高且无法补偿。

适合事件化的场景：
- 一个动作会触发多个后续动作。
- 跨服务协作较多。
- 需要审计历史。
- 需要多个读模型。
- 需要异步补偿和最终一致。


### 无状态 Web 与有状态业务

#### 1. Java Web 服务通常应该是无状态的
Java Web 程序大多数时候是无状态请求模型：一次 HTTP 请求进入 `Controller`，经过 `Service`、`Repository`，访问数据库或缓存后返回结果。请求结束后，线程释放，本次调用结束。

这里的“无状态”不是说业务没有状态，而是说应用实例本身不应该保存用户会话状态、业务流程状态或某个请求的中间状态。

服务无状态的好处是：
- 可以水平扩容。
- 任意请求可以打到任意实例。
- 应用重启不会丢业务状态。
- 发布、扩容、故障迁移更简单。
- 更适合负载均衡、容器化和微服务部署。

所以在 Java Web 系统里，`Controller`、`Service`、`Repository` 默认单例是合理的。它们应该承载业务行为，而不是保存业务状态。

#### 2. Spring 单例 Bean 不应该保存请求状态
Spring 中大多数 Bean 默认是单例，例如：

```java
@Service
public class OrderService {
    private final OrderRepository orderRepository;

    public OrderVO getOrder(Long orderId) {
        return orderRepository.findById(orderId);
    }
}
```

这种写法是安全的，因为 `OrderService` 不保存某个用户、某个订单、某次请求的可变状态。

危险的是在单例 Bean 中保存请求级状态：

```java
@Service
public class UserService {
    private Long currentUserId;
    private Long currentOrderId;
}
```

多个请求会共享同一个 `UserService` 实例，字段可能被不同线程互相覆盖，导致严重并发问题。

所以单例 Bean 中可以保存依赖、配置、策略对象、线程安全工具类，但不要保存：
- 当前用户 ID。
- 当前订单 ID。
- 当前流程步骤。
- 当前请求参数。
- 某个业务流程的中间状态。

这些状态应该通过方法参数、局部变量、请求上下文，或者外部存储来表达。

#### 3. 有状态请求不等于应该用 prototype
如果一个业务流程是有状态的，并不意味着 Spring Bean 应该改成 `prototype`。

`prototype` 只能让每次获取 Bean 时创建新对象，但它解决不了业务状态的核心问题：
- 不能跨请求保存状态。
- 不能跨 JVM 实例共享状态。
- 服务重启后状态会丢失。
- 无法支持恢复、重试、审计和补偿。

例如订单流程可能跨多个请求：

```text
创建订单 -> 支付 -> 发货 -> 确认收货 -> 退款
```

这个流程状态不应该放在 prototype Bean 里，而应该放在数据库状态机、事件流、工作流表或 Redis 短期状态中。

可以简单理解：
- `prototype` 适合单次使用的临时对象。
- `request scope` 适合当前请求上下文。
- `session scope` 适合传统 Web 会话。
- 业务长期状态应该外置化。

#### 4. 应用无状态，业务状态外置化
更合理的设计原则是：应用服务无状态，业务状态外置化。

也就是：

```text
Spring Bean 保存行为
数据库 / Redis / 事件流 / 工作流 保存状态
```

不同状态适合放在不同地方：
- 当前核心状态放数据库，例如订单状态、支付状态、退款状态。
- 短期临时状态放 Redis，例如验证码、防重复提交 token、支付处理中标记。
- 状态变化历史放事件表、流水表或 Kafka，例如 `OrderCreated`、`OrderPaid`。
- 复杂长流程放工作流或状态机，例如审批流、退款流、履约流。
- 查询视图可以通过事件异步构建，例如 ES 索引、报表表、用户订单列表视图。

这和流式系统的思想是一致的：应用实例本身不保留状态，状态以可持久、可重放、可恢复的形式存在于数据系统中。

#### 5. 有状态业务最常见的落地方式：状态机
大多数 Java 业务不需要一上来就做完整事件溯源，但非常适合做状态机。

例如订单状态：

```text
WAIT_PAY -> PAID -> SHIPPED -> COMPLETED
WAIT_PAY -> CANCELLED
PAID -> REFUNDING -> REFUNDED
```

每次状态更新都要带前置状态条件：

```sql
update orders
set status = 'PAID'
where id = ?
  and status = 'WAIT_PAY';
```

这样可以防止乱序事件、重复请求、并发任务错误覆盖状态。

例如支付成功和超时关单同时发生时：
- 支付成功只能把 `WAIT_PAY` 改成 `PAID`。
- 超时关单只能把 `WAIT_PAY` 改成 `CANCELLED`。
- 谁先成功，谁进入终态。
- 后到的操作影响行数为 0，不会覆盖已经形成的正确状态。

这就是用数据库状态机承载业务状态，而不是用 Java Bean 内存字段承载业务状态。

#### 6. 当前状态与事件历史要配合使用
只保存当前状态，查询方便，但历史过程不清楚。只保存事件历史，审计和重放能力强，但查询当前状态不方便。

实际 Java 系统中，更常见也更实用的是两者结合：
- 状态表保存当前状态，支撑在线查询。
- 事件表或流水表保存变化历史，支撑审计、对账、补偿和问题排查。

例如订单可以有：
- `orders`：保存当前订单状态。
- `order_status_log`：保存状态流转历史。
- `outbox_event`：保存待发布的领域事件。
- `order_event`：保存已经发生的业务事件。

这样既保留了传统数据库模型的查询便利，也吸收了流式系统的事件历史思想。

#### 7. 有状态流程可以用事件驱动柔性推进
对于跨服务的有状态流程，不要把所有步骤塞进一个刚性分布式事务里。

例如退款流程可以建模为：

```text
REFUND_CREATED
REFUNDING
REFUND_SUCCEEDED
REFUND_FAILED
REFUNDED
```

每个阶段由事件推进：
- 用户申请退款，订单服务创建退款单。
- 发布 `RefundRequested` 事件。
- 支付服务消费事件调用渠道退款。
- 渠道成功后发布 `RefundSucceeded`。
- 订单服务消费成功事件，把订单推进到 `REFUNDED`。
- 如果渠道超时，定时任务继续查询并补偿。

这种方式不是没有状态，而是把状态显式放到数据库状态机和事件流中，而不是藏在 Java 对象里。

#### 8. prototype、request scope、session scope 的边界
Spring 作用域可以解决对象生命周期问题，但不能替代业务状态建模。

`singleton` 适合无状态服务对象，是默认选择。

`prototype` 适合一次性、短生命周期、内部有临时计算状态的对象，但不适合保存订单、支付、审批这类业务状态。

`request scope` 适合当前 HTTP 请求内的上下文，例如当前用户、traceId、本次请求 metadata。请求结束后状态消失。

`session scope` 适合传统 Web 会话，但在前后端分离、微服务和多实例部署下使用要谨慎，通常需要 Redis Session 或 token 方案。

所以判断标准是：如果状态需要跨请求、跨实例、跨重启、可恢复、可审计，就不要放在 Bean 里。

#### 9. 典型错误设计
错误一：单例 Service 保存当前用户。

```java
@Service
public class UserService {
    private Long currentUserId;
}
```

多个请求会互相污染。

错误二：用 prototype 保存订单流程。

```java
@Scope("prototype")
@Component
public class OrderFlow {
    private Long orderId;
    private String step;
}
```

它无法跨请求、跨机器、跨重启恢复。

错误三：用 Redis 临时状态替代数据库核心状态。

```text
order:1001:status = PAID
```

Redis 可以做缓存或短期标记，但订单最终状态应该落数据库。

错误四：收到事件后不做状态保护。

```sql
update orders set status = 'PAID' where id = ?;
```

更好的方式是带前置状态：

```sql
update orders
set status = 'PAID'
where id = ?
  and status = 'WAIT_PAY';
```

#### 10. 和本章的关系
这一节和流式系统哲学的关系是：Java Web 服务本身应该尽量无状态，但业务世界天然有状态。关键不是把状态藏进 Bean，而是把状态显式建模到数据库、事件流、状态机和工作流中。

传统事务强调在一个边界内一次性修改状态。流式系统强调记录事件，让状态随着事件推进并最终收敛。

所以第 13 章对 Java 开发的一个重要启发是：

```text
无状态应用实例 + 有状态业务模型 + 事件驱动状态推进
```

应用实例越无状态，系统越容易扩展和恢复；业务状态越显式，系统越容易审计、补偿和演化。

### 我的理解
- 流式系统的哲学不是“用 MQ 替代数据库”，而是把系统从状态中心思维扩展到事件中心思维。
- 传统事务适合保护本地核心状态，事件驱动适合处理跨服务、后置动作和最终一致。
- Java 中的观察者模式、Spring 事件、MQ 消费，本质上都可以作为事件驱动的入口，但生产级事件驱动必须解决可靠投递和幂等消费。
- 柔性事务不是弱化正确性，而是承认分布式系统很难用一个大事务解决所有问题，于是通过事件、状态机、重试、补偿和对账保证最终正确。
- 事件是业务事实，不应该只是数据库字段变更通知。
- 状态表是当前视图，事件流是历史事实；关键业务最好两者都有。
- 真正落地事件驱动时，最重要的不是 MQ 框架，而是事件契约、状态机、幂等、outbox、失败处理和补偿机制。

### 可落地的实践启发
- 本地核心写入仍然使用 `@Transactional`，不要误以为事件驱动完全不要事务。
- 跨服务后置动作优先考虑事件驱动，而不是都放进分布式事务。
- 使用 outbox 模式保证业务状态和事件记录在同一个本地事务中提交。
- MQ 消费者必须幂等，默认消息会重复。
- 事件要表达业务事实，不要直接暴露数据库 Entity。
- 状态更新要带前置状态条件，防止乱序事件覆盖正确结果。
- 对长流程业务设计中间状态，例如 `PROCESSING`、`WAIT_CONFIRM`、`FAILED`。
- 每个失败事件都要有重试、死信、告警和补偿路径。
- 对关键业务保留流水或事件历史，方便审计、对账和恢复。
- 复杂查询和报表可以通过事件构建独立读模型。
- 事件驱动系统要配合批处理对账，保证最终收敛。
- 不要过度事件化，简单单库事务能解决的问题就保持简单。

### 一句话总结
流式系统的哲学，是把系统从“只维护当前状态”扩展为“记录持续发生的业务事件，并从事件中派生状态”；对 Java 开发来说，它最重要的落地价值不是炫技，而是用领域事件、outbox、MQ、状态机、幂等消费和补偿对账，把一部分跨服务刚性事务改造成更适合分布式系统的柔性事务。


