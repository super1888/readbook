# 《设计数据密集型应用》读书笔记

## 第 8 章：事务

### 本章核心
这一章讲的是事务，也就是数据库如何把多个读写操作组合成一个逻辑单元，并在并发、失败、崩溃等复杂情况下，尽量让应用看到一个更简单、更可靠的世界。对 Java 后端开发来说，最常见的实践就是在业务方法上使用 `@Transactional`，让一组数据库操作要么全部成功，要么全部失败。

但这一章真正重要的不是只记住 ACID 四个字母，而是理解事务在解决什么问题、不同隔离级别分别牺牲了什么、MVCC 为什么能提升并发性能、乐观锁和悲观锁适合什么场景，以及分布式事务为什么复杂。

### 我读完后的直观感受
这一章对 Java 开发来说很贴近日常。平时写业务代码时，经常会觉得事务就是加一个 `@Transactional` 注解。但实际项目里，很多问题正是因为事务没有生效、隔离级别理解不清、并发更新没有控制、跨服务操作没有补偿，最后导致数据错乱。

比如：
- 方法上忘记加 `@Transactional`，多条 SQL 中间失败后，前面的写入已经提交。
- 同类内部方法调用导致 `@Transactional` 不生效。
- 异常被 `try-catch` 吃掉，事务没有回滚。
- 默认只对运行时异常回滚， checked exception 没有触发回滚。
- 多线程、异步任务、消息回调里以为还在同一个事务，其实已经不是。

另外，这一章也让我对 MVCC、快照隔离、SSI、2PC 这些概念有了更系统的理解。尤其是 SSI，准确说应该是 Serializable Snapshot Isolation，不是 SSL。SSL 是安全传输协议，而 SSI 是一种基于快照隔离的可串行化实现，可以理解为一种偏乐观的并发控制方式。

### 关键概念

#### 1. 事务为什么存在
事务的作用是把多个操作包装成一个整体，让应用不用在每一步都手动处理失败和并发问题。

例如创建订单可能包括：
- 写入订单主表。
- 写入订单明细。
- 扣减库存。
- 写入支付流水。
- 记录操作日志。

如果没有事务，中间任何一步失败，都可能留下半成功状态。例如订单创建了，但明细没写；库存扣了，但订单没创建。

事务让开发者可以把这些操作当成一个整体：成功就全部提交，失败就全部回滚。

对 Java 开发来说，典型写法是：

```java
@Transactional(rollbackFor = Exception.class)
public void createOrder(CreateOrderCommand command) {
    orderRepository.save(order);
    orderItemRepository.saveItems(items);
    inventoryRepository.deduct(command.getSkuId(), command.getQuantity());
}
```

但注解只是入口，真正要理解的是事务边界和事务是否真的生效。

#### 2. ACID
ACID 是事务的四个经典特性：原子性、一致性、隔离性、持久性。

原子性指事务中的操作要么全部成功，要么全部失败。它重点处理失败恢复问题，而不是并发问题。

一致性更多是应用层和数据库约束共同维护的结果。数据库可以提供外键、唯一约束、事务等能力，但业务规则是否一致，仍然需要应用代码保证。

隔离性指多个并发事务之间不会互相造成不可接受的干扰。隔离级别越高，越接近串行执行，但性能成本也越高。

持久性指事务提交后，数据不会因为数据库崩溃而轻易丢失。它依赖 WAL、redo log、复制、刷盘策略等机制。

我的理解是：ACID 不是一个绝对标准，而是一组目标。不同数据库、不同隔离级别、不同配置下，事务提供的保证并不完全一样。

#### 3. Java 中的事务边界
在 Spring 里，`@Transactional` 通常通过代理实现。也就是说，并不是方法上写了注解就一定生效。

常见不生效场景包括：
- 注解加在 `private` 方法上。
- 同一个类内部直接调用事务方法。
- 方法不是 `public`。
- 当前类没有被 Spring 管理。
- 异常被捕获后没有继续抛出。
- 默认情况下 checked exception 不触发回滚。
- 数据库引擎不支持事务，例如 MyISAM。
- 异步线程中执行的逻辑已经不在原事务上下文里。

例如：

```java
public void outer() {
    this.inner(); // 同类内部调用，可能绕过 Spring 代理
}

@Transactional
public void inner() {
    // 事务可能不生效
}
```

所以在 Java 项目中，事务不仅是数据库概念，也是框架代理、异常传播、方法可见性和调用路径共同决定的结果。

#### 4. 隔离级别
事务隔离级别用来描述并发事务之间互相可见的程度。

常见隔离级别包括：
- 读未提交：可能读到其他事务未提交的数据。
- 读已提交：只能读到已经提交的数据。
- 可重复读：同一事务内多次读取同一数据，结果保持一致。
- 可串行化：效果等价于事务一个一个串行执行。

隔离级别越高，并发异常越少，但实现成本越高，可能导致更多锁等待、冲突检测或事务重试。

MySQL InnoDB 默认隔离级别通常是可重复读。它通过 MVCC 和锁机制避免很多并发问题，但这并不意味着所有业务并发问题都会自动消失。

#### 5. 脏读、脏写、不可重复读、幻读
脏读指一个事务读到了另一个事务尚未提交的数据。如果对方回滚，读到的数据就是无效的。

脏写指一个事务覆盖了另一个尚未提交事务写入的数据。这通常必须被数据库阻止，否则回滚和数据恢复会很混乱。

不可重复读指同一个事务内，两次读取同一行数据，结果不一样。原因是中间有其他事务提交了修改。

幻读指同一个事务内，两次按照相同条件查询，第二次出现了第一次不存在的新行。例如第一次查询没有符合条件的订单，第二次查询出现了一条新订单。

这些问题本质上都是并发事务互相干扰造成的。隔离级别不同，能防住的问题也不同。

#### 6. MVCC
MVCC 是多版本并发控制。它的核心思想是：读操作不一定要阻塞写操作，写操作也不一定要阻塞读操作，数据库可以通过保存数据的多个版本，让事务读取自己应该看到的那个版本。

在 MySQL InnoDB 中，MVCC 和 undo log、事务 ID、read view 等机制相关。一个事务开始读取时，会根据快照判断哪些版本对自己可见。

MVCC 的好处是提升并发性能。普通快照读可以不加锁，不会阻塞正在修改同一行的事务。

例如一个事务正在更新用户昵称，另一个事务仍然可以读取旧版本昵称，而不是一直等待更新事务提交。

但 MVCC 也有边界：
- 它主要解决一致性读问题。
- 当前读、加锁读、更新操作仍然会涉及锁。
- 它不能替代业务层的并发控制。
- 在高并发更新同一行时，仍然可能出现锁等待或更新冲突。

#### 7. 快照隔离
快照隔离指事务读取的是某个时间点的数据快照。事务执行过程中，即使其他事务提交了修改，当前事务仍然看到自己的快照。

这可以避免不可重复读，让读操作非常稳定。

但快照隔离不等于可串行化。它仍然可能出现写偏斜等并发异常。

例如两个医生值班，规则是至少要有一个医生值班：
- 事务 A 看到医生 1 和医生 2 都在值班，于是把医生 1 改为不值班。
- 事务 B 也看到两个医生都在值班，于是把医生 2 改为不值班。
- 两个事务分别修改不同的行，都能提交。
- 最终没有医生值班，违反业务约束。

这说明只靠快照读，不能保证所有跨多行的业务不变量。

#### 8. 乐观锁
乐观锁假设冲突不常发生，所以不提前加锁，而是在提交更新时检查数据有没有被别人改过。

Java 业务中最常见的是 `@Version` 或手动版本号字段：

```sql
update product
set stock = stock - 1,
    version = version + 1
where id = ?
  and stock > 0
  and version = ?;
```

如果返回影响行数为 0，说明数据已经被别人改过，当前操作失败或需要重试。

乐观锁可以在一些场景下减少显式事务依赖，尤其是单行条件更新。例如扣库存时，通过 `where stock > 0` 和 `version` 控制并发，单条 SQL 本身就是原子的。

但不能简单认为乐观锁可以替代所有事务。只要一个业务操作涉及多条 SQL、多张表、多个状态变更，仍然通常需要事务保证整体一致。

适合乐观锁的场景：
- 冲突不高。
- 更新目标明确。
- 可以失败重试。
- 单行或少量数据更新。
- 业务允许通过版本号判断并发冲突。

不适合单纯依赖乐观锁的场景：
- 多表强一致写入。
- 复杂状态流转。
- 高冲突热点更新。
- 无法接受重试失败的核心流程。

#### 9. 悲观锁
悲观锁假设冲突很可能发生，所以在读取或修改前先锁住数据。

MySQL 中常见写法是：

```sql
select * from account where id = ? for update;
```

在事务中执行 `for update` 后，其他事务如果想修改同一行，通常需要等待当前事务提交或回滚。

悲观锁适合冲突较高、必须严格串行处理的场景，例如账户余额、库存核心扣减、任务抢占等。

但悲观锁也有代价：
- 容易降低并发性能。
- 事务时间过长会导致锁等待。
- 多资源加锁顺序不一致可能导致死锁。
- 锁范围可能比预期更大，尤其是索引设计不合理时。

所以使用悲观锁时，一定要让事务尽量短，并确保查询命中合适索引。

#### 10. 写偏斜
写偏斜是快照隔离下比较典型的问题。它不是两个事务修改同一行，而是两个事务基于同一个条件读取，然后分别修改不同的行，最终破坏整体约束。

例如会议室预订、医生值班、资源名额、审批人数限制，都可能出现类似问题。

解决方式包括：
- 使用可串行化隔离级别。
- 对相关范围显式加锁。
- 把跨多行约束转成单行约束。
- 使用唯一索引或约束让数据库帮忙兜底。
- 在业务层用乐观锁或版本号保护聚合根。

对 Java 开发来说，这提醒我们：事务隔离不是只关心单行更新，业务不变量往往跨多行、多表存在。

#### 11. 可串行化
可串行化是最强的隔离级别，效果等价于所有事务按某种顺序一个一个执行。

实现方式可以有多种：
- 真正串行执行。
- 两阶段锁。
- 可串行化快照隔离。

可串行化能避免很多复杂并发异常，但成本也更高。实际业务里不一定所有操作都需要最高隔离级别，否则系统吞吐会明显下降。

更常见的做法是：默认使用数据库常规隔离级别，然后在关键业务点通过唯一约束、条件更新、乐观锁、悲观锁、状态机等手段补强。

#### 12. SSI
SSI 是 Serializable Snapshot Isolation，也就是可串行化快照隔离。它可以理解为在快照隔离基础上增加冲突检测，从而达到可串行化效果。

它和乐观锁有相似思路：事务先正常执行，不一开始就阻塞所有可能冲突的操作，而是在执行过程中记录读写依赖，提交时如果发现可能破坏串行化，就中止其中一个事务。

所以你提到“第一次接触 SSL，这个也是乐观锁模式”，这里更准确应该是 SSI。它确实带有乐观并发控制的味道，因为它允许事务先并发执行，再通过冲突检测决定是否提交。

SSI 的启发是：并发控制不一定只有加锁一种方式，也可以通过版本、快照、依赖关系和提交时校验来实现。

#### 13. 2PC
2PC 是两阶段提交，用来在多个参与者之间协调事务提交。

它大致分为两个阶段：
- 准备阶段：协调者询问所有参与者是否可以提交。
- 提交阶段：如果所有参与者都同意，协调者通知提交；否则通知回滚。

2PC 的目标是让多个节点在提交或回滚上达成一致。

但 2PC 的问题也很明显：
- 协调者可能成为单点。
- 参与者在准备后需要等待协调者决定。
- 网络故障时可能长时间阻塞。
- 实现和运维复杂度较高。

在 Java 业务里，XA 事务、部分分布式事务框架会涉及 2PC 思想。但在微服务场景中，很多团队会避免在核心链路上大量使用强 2PC，而是选择 TCC、Saga、可靠消息、最终一致性等方式。

#### 14. 分布式事务与 Seata
单库事务只覆盖一个数据库连接内的操作。但微服务系统中，一个业务动作可能跨多个服务、多个数据库。

例如下单流程可能包括：
- 订单服务创建订单。
- 库存服务扣减库存。
- 优惠券服务核销优惠券。
- 账户服务冻结余额。

这些操作不在同一个本地事务里，就需要分布式一致性方案。

Seata 是 Java 生态中常见的分布式事务框架，常见模式包括 AT、TCC、Saga、XA。

TCC 模式把业务分成三个阶段：
- Try：预留资源。
- Confirm：确认执行。
- Cancel：取消释放。

例如库存扣减可以设计为：
- Try：冻结库存。
- Confirm：真正扣减库存。
- Cancel：释放冻结库存。

TCC 的优点是业务可控，适合对一致性要求较高的分布式场景。缺点是开发成本高，每个服务都要实现 Try、Confirm、Cancel，并处理幂等、空回滚、悬挂等问题。

#### 15. 最终一致性
很多分布式业务并不一定要强事务，而是可以接受最终一致性。

例如下单后发送积分、发送通知、同步搜索索引、更新统计报表，这些通常不需要和订单创建处于同一个强事务里。

常见做法包括：
- 本地事务写业务表和消息表。
- 事务提交后发送 MQ。
- 消费者幂等处理消息。
- 失败后重试。
- 定时任务扫描补偿。

这种方式牺牲了即时一致性，但换来了更好的可用性和系统解耦。

对 Java 开发来说，要学会区分哪些操作必须强一致，哪些操作可以最终一致。不是所有跨服务操作都应该上分布式事务。

### 架构中的主要权衡

#### 正确性 vs 性能
隔离级别越高，越能避免并发异常，但性能成本也越高。业务需要根据数据重要性选择合适方案。余额、库存、支付状态要更重视正确性，报表、日志、通知可以接受更弱一致性。

#### 简单本地事务 vs 复杂分布式事务
单库本地事务简单可靠，开发成本低。分布式事务可以覆盖跨服务操作，但引入协调、补偿、幂等、超时和故障恢复问题。能用本地事务解决的，尽量不要拆成分布式事务。

#### 乐观锁 vs 悲观锁
乐观锁适合冲突少、可以重试的场景，性能较好。悲观锁适合冲突高、必须严格串行的场景，但会降低并发能力。选择时要看冲突概率和业务能否接受失败重试。

#### 强一致 vs 最终一致
强一致让业务理解简单，但对性能、可用性和系统耦合要求更高。最终一致更适合分布式系统，但需要补偿、重试、幂等和对账能力。

#### 框架自动管理 vs 理解底层边界
`@Transactional` 能简化开发，但不能替代对事务边界的理解。事务是否生效、何时提交、异常是否回滚、是否跨线程、是否跨数据源，都需要开发者自己判断。

### 对日常 Java 开发的实际帮助

#### 1. 更谨慎使用 `@Transactional`
事务注解不是随便加，也不是加了就一定生效。需要关注方法可见性、调用方式、异常类型、传播行为和数据库连接。

特别是服务方法中有多条写 SQL 时，应该明确事务边界。否则一旦中间失败，就可能出现部分写入成功、部分写入失败。

#### 2. 不要用大事务包住所有逻辑
事务时间越长，锁持有时间越长，也越容易造成阻塞和死锁。

事务内应该尽量只放数据库关键操作，不要在事务里做：
- 远程 HTTP 调用。
- MQ 同步等待。
- 大文件处理。
- 长时间计算。
- 用户交互等待。

远程调用放在事务中尤其危险，因为对方慢会导致本地数据库锁长时间不释放。

#### 3. 用数据库约束兜底业务正确性
业务代码判断不是完全可靠的，因为并发下多个请求可能同时通过判断。

例如防重复下单，不能只在 Java 里先查有没有订单，再决定是否插入。更稳妥的是使用唯一索引、幂等键或状态机约束。

数据库约束是最后一道防线。

#### 4. 合理使用乐观锁
`@Version` 或版本号字段适合处理并发更新。例如更新商品库存、修改配置、编辑文章时，可以通过版本号避免覆盖他人修改。

但乐观锁失败后，要设计清楚处理方式：
- 自动重试。
- 返回用户重新提交。
- 放弃当前操作。
- 进入补偿流程。

不能只加版本号，却不处理更新失败。

#### 5. 正确认识 MySQL 默认隔离级别
MySQL InnoDB 默认可重复读已经能解决很多问题，但不是所有并发业务都安全。

例如跨多行约束、库存超卖、重复创建、状态机乱序，仍然需要唯一索引、条件更新、锁或业务幂等来保证。

#### 6. 分布式事务要谨慎引入
Seata、TCC、2PC 不是银弹。它们能解决一部分跨服务一致性问题，但也会增加开发、运维和故障处理成本。

很多场景更适合最终一致性：本地事务 + MQ + 幂等消费 + 补偿任务。

#### 7. 所有重试都要考虑幂等
事务失败、网络超时、2PC 阻塞、TCC Confirm 重复调用、MQ 重复投递，都可能导致同一个操作执行多次。

所以核心接口要设计幂等：
- 请求号。
- 业务单号。
- 唯一索引。
- 幂等表。
- 状态机判断。

没有幂等，事务和重试机制反而可能放大错误。

### 实践举例

#### 1. 创建订单必须开启事务
创建订单通常至少要写订单主表和订单明细表。如果不加事务，可能出现主表写入成功，明细写入失败。

更好的做法：
- 在应用服务层开启 `@Transactional`。
- 事务内完成订单主表、明细表、必要流水写入。
- 使用 `rollbackFor = Exception.class` 明确回滚范围。
- 事务提交后再发送 MQ 或执行后置异步动作。

#### 2. 扣库存可以用条件更新
库存扣减不一定每次都要先查再改，可以用单条条件更新降低并发风险：

```sql
update sku_stock
set stock = stock - #{quantity}
where sku_id = #{skuId}
  and stock >= #{quantity};
```

如果影响行数为 1，说明扣减成功；如果为 0，说明库存不足或并发下已经被扣完。

这种方式利用了数据库单条更新的原子性，可以减少先查再改带来的并发窗口。

#### 3. 编辑配置使用乐观锁
后台配置编辑经常出现两个人同时打开同一条配置，然后先后保存。没有乐观锁时，后保存的人会覆盖前一个人的修改。

更好的做法：
- 表中增加 `version` 字段。
- 查询时返回版本号。
- 更新时带上版本号条件。
- 更新失败时提示用户配置已被他人修改。

这类场景冲突不高，乐观锁比较合适。

#### 4. 账户转账使用事务和锁
同库账户转账需要同时扣 A 账户、加 B 账户，并记录流水。

更好的做法：
- 在同一个本地事务中完成。
- 按固定顺序锁定账户，降低死锁概率。
- 使用余额条件防止扣成负数。
- 流水号加唯一索引，保证幂等。

如果 A 和 B 分布在不同服务或不同库，就要考虑 TCC、Saga 或最终一致性方案。

#### 5. TCC 处理跨服务资源预留
以下单扣库存为例，TCC 可以拆成：
- Try：冻结库存。
- Confirm：订单确认后扣减冻结库存。
- Cancel：订单失败后释放冻结库存。

实现时必须处理：
- Try 重复调用。
- Confirm 重复调用。
- Cancel 重复调用。
- Try 没执行但 Cancel 先到的空回滚。
- Cancel 已执行后 Try 又到的悬挂问题。

所以 TCC 的难点不只是三段方法，而是异常情况下的幂等和状态管理。

#### 6. 本地事务加 MQ 实现最终一致
订单创建成功后发送积分消息，可以不和订单放在一个分布式强事务中。

更好的做法：
- 本地事务中写订单表和消息表。
- 事务提交后投递 MQ。
- MQ 消费者给用户加积分。
- 消费者根据消息 ID 幂等处理。
- 定时任务扫描未发送或失败消息补偿。

这样可以避免订单服务和积分服务强耦合，同时通过重试和补偿保证最终一致。

### 我的理解
- 事务的价值是把失败和并发复杂度尽量封装起来，但它不能消除所有业务一致性问题。
- `@Transactional` 是 Java 开发最常用的事务入口，但必须理解代理、生效条件、异常回滚和事务边界。
- MVCC 让读写并发能力更好，但它不是业务并发控制的万能解法。
- 乐观锁可以在部分场景下用版本号实现无显式事务的并发读写控制，但多表、多步骤业务仍然需要事务。
- MySQL 默认隔离级别能解决很多常见问题，但复杂业务不变量还需要唯一约束、条件更新、锁、状态机和幂等配合。
- SSI 不是 SSL，它是一种可串行化快照隔离，思路上接近乐观并发控制。
- 2PC 能协调多个参与者提交，但会引入阻塞、协调者和故障恢复问题。
- Seata、TCC 等分布式事务方案有价值，但不能滥用；很多场景最终一致性更适合。
- 对 Java 后端来说，事务不是只加注解，而是要明确哪些操作必须原子提交，哪些操作可以异步补偿。

### 可落地的实践启发
- 多条写 SQL 构成一个业务动作时，优先明确 `@Transactional` 事务边界。
- 使用 `@Transactional(rollbackFor = Exception.class)` 避免 checked exception 不回滚的问题。
- 不要在同类内部调用事务方法，避免绕过 Spring 代理。
- 不要在事务中执行耗时远程调用，减少锁等待和长事务风险。
- 核心并发写入要用唯一索引、条件更新、乐观锁或悲观锁兜底。
- 使用 `@Version` 或版本号后，必须处理更新失败和重试策略。
- 库存、余额、支付状态等关键数据，不要只依赖先查再改。
- 跨服务一致性优先判断是否真的需要强事务。
- 能本地事务解决的，不要轻易拆成分布式事务。
- 使用 TCC 时必须处理幂等、空回滚、悬挂和超时补偿。
- MQ 最终一致方案要配合本地消息表、幂等消费和补偿任务。
- 所有可能重试的接口都要设计幂等键或业务唯一约束。

### 一句话总结
事务的核心不是简单地加一个 `@Transactional`，而是在失败和并发条件下维护业务正确性；本地事务、MVCC、乐观锁、悲观锁、SSI、2PC、TCC 和最终一致性都是不同层次的工具，真正重要的是根据业务一致性要求选择合适的事务边界和并发控制方式。
