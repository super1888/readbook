# 《设计数据密集型应用》读书笔记

## 第 5 章：编码与演化

### 本章核心
这一章讲的是数据如何从程序内存中的对象，转换成可以存储、传输和被其他系统理解的格式，以及这些格式如何支持系统长期演化。对 Java 后端开发来说，重点不是记住 JSON、XML、Protobuf、Thrift、Avro 的所有细节，而是理解接口、消息、缓存和历史数据为什么必须考虑新旧版本共存。

### 我读完后的直观感受
平时写 Java 接口时，大多数人只是机械地使用 `Controller`、`DTO`、`JSON`、`RESTful`、`Feign`、`MQ`，很少认真思考为什么字段不能随便删、为什么新增字段通常比较安全、为什么老版本客户端会影响接口设计。读完这一章会发现，所谓编码格式并不是一个底层细节，而是系统边界上的长期契约。

很多团队使用 REST 和 JSON，并不一定是因为真正理解 REST 的架构思想，而是因为它们简单、通用、容易调试、生态成熟、跨语言成本低。大家都这么做，是因为它在工程协作上足够实用；但如果只会机械使用，而不理解兼容性和演化问题，系统复杂后就很容易踩坑。

### 关键概念

#### 1. 编码与解码
程序运行时，数据通常是内存中的对象，例如 Java 里的实体、DTO、集合和枚举。但只要数据要写入文件、数据库、缓存、消息队列，或者通过网络传给另一个服务，就必须转换成某种字节序列。

这个转换过程就是编码，也可以叫序列化。反过来，从字节序列还原成程序对象，就是解码，也可以叫反序列化。

对 Java 开发来说，最常见的是：
- Java 对象编码成 JSON，作为 HTTP 响应返回给前端。
- HTTP 请求体中的 JSON 解码成 Java `RequestDTO`。
- MQ 消息中的 JSON、Avro 或 Protobuf 解码成事件对象。
- 缓存中的字符串或二进制数据解码成业务对象。

框架把这些过程封装得太好，所以我们容易忘记：跨进程之后，真正稳定存在的不是 Java 类，而是外部数据格式。

#### 2. 编码格式是系统契约
在单体应用内部，修改一个 Java 类通常只影响当前代码库。但一旦这个类被用于接口返回、MQ 消息、缓存结构或持久化数据，它就变成了系统之间的契约。

例如一个接口原来返回：

```json
{
  "id": 1,
  "name": "Alice"
}
```

后来新增字段：

```json
{
  "id": 1,
  "name": "Alice",
  "age": 20
}
```

这看起来只是 Java DTO 增加了一个属性，但实际上影响的是所有消费者：前端、移动端、其他后端服务、测试脚本、数据同步任务、历史消息消费者等。

所以数据格式不是实现细节，而是边界协议。它决定了不同版本代码能不能同时运行。

#### 3. 向后兼容
向后兼容指的是：新代码可以读取旧代码写出的数据。

例如旧数据没有 `age` 字段，新代码增加了 `age` 字段。如果新代码允许 `age` 为空，或者能提供默认值，那么它就可以处理旧数据。

这在真实系统里很重要，因为新版本服务上线后，仍然可能遇到：
- 数据库里的旧格式数据。
- Redis 里的旧缓存。
- MQ 里积压的旧消息。
- 老服务写出的旧格式数据。

如果新代码不能理解旧数据，系统升级就会变得非常危险。

#### 4. 向前兼容
向前兼容指的是：旧代码可以读取新代码写出的数据。

例如新服务返回了一个旧客户端不认识的 `age` 字段。如果旧客户端可以忽略未知字段，它就还能正常运行。

这在滚动发布和灰度发布中特别重要。生产环境通常不是所有服务瞬间同时升级，而是新旧版本在一段时间内共存。如果旧代码不能处理新数据，那么新版本一上线就可能影响还没升级的实例或客户端。

对 JSON 来说，很多解析库默认会忽略未知字段，所以新增字段通常比较安全。但这不是绝对的，如果反序列化配置成遇到未知字段就报错，新增字段也会变成破坏性变更。

#### 5. 新增字段通常安全，删除字段通常危险
日常开发里常说接口字段“只能加，不能随便删”，背后的原因就是兼容性。

新增字段通常比较安全，因为：
- 新代码可以给旧数据中的缺失字段设置默认值。
- 旧代码可以忽略不认识的新字段。
- 消费方不使用新字段时，不影响原有逻辑。

删除字段通常比较危险，因为：
- 老客户端可能仍然依赖这个字段。
- 下游服务可能正在用这个字段做判断。
- MQ 消费者可能还需要这个字段。
- 缓存或历史数据中可能仍然存在旧结构。

修改字段类型也很危险。例如把数字类型的 `id` 改成字符串，可能导致反序列化失败、校验失败、SQL 条件异常或前端展示异常。

最隐蔽的是修改字段语义。字段名和类型都没变，但业务含义变了，比如 `status = 1` 以前表示启用，现在表示审核中。这类变化不会被编译器发现，但可能造成严重业务错误。

#### 6. 语言内置序列化的问题
很多语言都有自己的对象序列化机制，例如 Java 原生序列化。但这类格式通常不适合作为长期存储或跨服务通信协议。

主要问题包括：
- 和特定语言强绑定，跨语言支持差。
- 容易暴露语言内部类结构。
- 类名、字段、继承关系变化后容易破坏兼容性。
- 安全风险较高。
- 长期维护和排查困难。

如果把 Java 对象直接序列化后放到 Redis、文件或 MQ 中，后续类结构一改，就可能读不出来。更稳妥的做法是使用明确的数据交换格式，例如 JSON、Protobuf、Avro、Thrift，并把它们当成外部契约维护。

#### 7. JSON、XML 与二进制编码
JSON 的优点是简单、可读、易调试、跨语言生态好，非常适合 Web API 和普通业务接口。缺点是体积较大，类型约束弱，对 schema 和兼容性的管理更多依赖团队规范。

XML 表达能力强，也有 schema 支持，但格式冗长，解析复杂，在现代 Web API 中使用频率已经下降。不过在传统企业系统和部分标准协议中仍然常见。

Protobuf、Thrift 这类二进制编码格式体积小、性能好、有明确 schema，并且支持代码生成，更适合内部 RPC、高性能通信和多语言服务协作。缺点是可读性差，调试不如 JSON 方便，也需要团队认真维护 schema。

Avro 更强调 schema 演化，在日志、大数据、Kafka 等场景中比较常见。它适合处理长期保存的数据，因为读取数据时可以结合写入时的 schema 和读取时的 schema，从而更好地适配格式变化。

#### 8. REST 与 RPC
REST 理想上是围绕资源设计接口，例如：

```http
GET /users/1
POST /users
PUT /users/1
DELETE /users/1
```

它强调资源、URI、HTTP 方法、状态码和表述。REST 适合对外 API、前后端交互和需要容易调试的业务接口。

RPC 更接近调用远程方法，例如：

```java
User getUser(Long id);
Order createOrder(CreateOrderCommand command);
void cancelOrder(Long orderId);
```

它强调服务、方法、参数、返回值和强类型契约，更适合内部服务调用、高性能通信和代码生成。

现实中很多所谓 RESTful 接口，其实只是 `HTTP + JSON + 动作路径`，例如：

```http
POST /order/cancel
POST /user/queryList
POST /payment/confirm
```

这不一定完全错误，因为很多业务动作确实很难自然建模成资源。真正重要的是团队能否保持接口风格一致、语义清楚、兼容性可控，而不是形式上是否绝对符合 REST。

#### 9. 消息队列中的格式演化
MQ 消息比 HTTP 接口更容易被忽略兼容性问题，因为消息不是即时消费的。消息可能延迟、重试、堆积、回放，也可能被多个消费者同时订阅。

生产者升级后，老消费者可能还在运行；消费者升级后，队列里可能还有旧消息。因此 MQ 事件格式要比普通接口更谨慎。

设计 MQ 消息时要注意：
- 事件字段尽量稳定。
- 新增字段要允许消费者忽略。
- 删除字段要有较长废弃周期。
- 不要随便改变字段语义。
- 消费者要能处理旧版本消息。
- 重要事件可以考虑版本号或 schema 管理。

#### 10. 数据库和缓存里的旧数据
编码演化不只发生在网络接口中，也发生在数据库和缓存中。

例如数据库中某个 JSON 字段原来是：

```json
{
  "name": "Alice",
  "age": 20
}
```

后来改成：

```json
{
  "fullName": "Alice",
  "birthday": "2006-01-01"
}
```

新代码上线后，仍然可能读到旧格式数据。这时就需要考虑读取兼容、默认值、数据迁移、双写、灰度发布和回滚方案。

所以系统演化不是只改代码，还包括已有数据如何被新代码理解。

### 架构中的主要权衡

#### 可读性 vs 性能
JSON 可读性强，调试方便，但体积较大，解析成本也更高。Protobuf、Thrift 等二进制格式性能更好，但不便于人工查看和排查问题。对外接口通常更看重通用性和可调试性，内部高频通信可以更关注性能和契约约束。

#### 灵活性 vs 约束
JSON 结构灵活，新增字段方便，但也容易因为缺少强 schema 导致接口变更不受控。Protobuf、Avro 等格式约束更强，能让契约更清晰，但也要求团队维护 schema 文件和生成代码流程。

#### 短期开发效率 vs 长期演化能力
直接复用 Entity、随手改 DTO、接口不写版本，短期开发很快。但系统一旦被多个调用方依赖，后续每次变更都会变得困难。把接口模型、领域模型和存储模型分开，短期多写一些代码，长期更容易演化。

#### REST 的通用性 vs RPC 的强契约
REST/JSON 通用、简单、容易调试，适合前后端和开放接口。RPC 更强类型、更高效，适合内部服务通信。选型不应该只看流行程度，而要看调用方是谁、性能要求多高、调试是否重要、契约是否需要强约束。

#### 兼容性 vs 清理成本
为了兼容旧客户端、旧消息和旧数据，系统往往需要保留旧字段、旧逻辑和默认值。这会增加代码复杂度。但如果过早删除旧格式，又会破坏线上稳定性。成熟系统需要给字段和接口设计生命周期，而不是随意增删。

### 对日常 Java 开发的实际帮助

#### 1. 更重视 DTO 的边界意义
DTO 不是普通 Java 类，一旦出现在接口、MQ、缓存或持久化数据中，它就是外部契约。修改 DTO 前要先想清楚谁在读、谁在写、是否有旧版本、是否有历史数据。

#### 2. 不要直接暴露 Entity
直接把数据库实体作为接口返回，会让数据库结构变化影响外部 API。更好的做法是区分 Entity、Domain Model、Request、Response、DTO、VO 和 Event，让内部模型和外部契约解耦。

#### 3. 理解为什么新增字段比删除字段安全
新增字段通常可以通过默认值和忽略未知字段实现兼容，但删除字段、修改类型、改变语义都会影响已有消费者。以后改接口时，不能只看当前代码能不能编译通过。

#### 4. 更谨慎地设计 MQ 事件
MQ 消息可能长期存在，也可能被多个系统消费。事件一旦发出去，就很难像方法调用一样控制所有调用方。因此事件字段应该稳定、语义清楚，并且最好有版本意识。

#### 5. 不再机械理解 RESTful
REST/JSON 是一种工程上很实用的默认选择，但不是唯一正确答案。对外 API 可以优先考虑 REST 的通用性，内部高性能服务可以考虑 RPC 或二进制协议。关键不是形式，而是接口语义清楚、契约稳定、演化可控。

#### 6. 发布时考虑新旧版本共存
生产环境常见的是滚动发布、灰度发布和可回滚发布。发布过程中，新旧代码会同时存在。因此接口、消息和数据格式都要支持一段时间内的新旧版本共存。

### 我的理解
- 编码格式不是简单的序列化工具，而是系统之间交换数据的契约。
- Java 框架屏蔽了很多编码细节，但不能因此忽略跨服务、跨语言、跨版本的数据兼容问题。
- REST 和 JSON 流行，是因为它们在通用性、可读性、调试体验和生态成熟度之间取得了很好的平衡，而不是因为它们在所有场景下最优。
- 真正危险的接口变更往往不是新增字段，而是删除字段、修改类型和改变字段语义。
- 系统升级不是瞬间完成的，新旧代码、新旧数据、新旧消息都会在一段时间内共存。
- 成熟的系统设计要默认未来会变化，因此接口、事件和数据格式都要有演化能力。

### 可落地的实践启发
- 对外接口不要直接返回数据库 Entity。
- Request、Response、DTO、Event 都要当成系统契约维护。
- 新增字段尽量设计为可选字段，并提供默认值或空值处理。
- 不随便删除字段，先标记废弃，再等待调用方迁移，最后再删除。
- 不随便修改字段类型，必要时新增字段替代旧字段。
- 不在原字段上偷偷改变业务语义。
- 消费方要尽量忽略未知字段，提高向前兼容能力。
- 生产方要考虑老消费者、旧客户端和历史数据是否还能处理。
- MQ 事件要比普通 HTTP 接口更重视版本和兼容性。
- 缓存和数据库中的旧数据也要纳入升级方案。
- 重要接口要有文档、版本、变更记录和废弃流程。
- 内部高性能通信可以考虑 Protobuf、gRPC、Thrift 等强契约方案。
- 对外开放 API 优先考虑 JSON/HTTP 的通用性和可调试性。

### 一句话总结
编码与演化的核心不是比较 JSON、REST、Protobuf 谁更好，而是理解数据格式是系统边界上的长期契约；好的接口和消息设计，应该允许新旧代码、新旧数据、新旧服务在一段时间内安全共存。
