# 《设计数据密集型应用》读书笔记

## 第 10 章：一致性与共识

### 本章核心
这一章讲的是分布式系统里最难的一类问题：当系统由多台机器组成，并且网络、节点、时钟都可能出问题时，如何让多个节点对某件事达成一致。

前面几章讲复制、分片、事务、分布式系统的麻烦，更多是在描述问题；这一章开始触及解决这些问题的理论基础：线性一致性、顺序保证、全序广播、共识、两阶段提交、领导者选举、分布式锁和成员关系。

对 Java 开发来说，平时不一定会直接实现 Paxos、Raft、ZAB 这类共识算法，但会大量使用建立在它们之上的组件，比如 Nacos、ZooKeeper、etcd、Consul、Redis Sentinel、Redis Cluster、Kafka、数据库高可用组件、分布式任务调度系统等。

所以这一章真正要理解的是：很多中间件之所以能完成注册中心、配置中心、主从切换、分布式锁、任务选主，本质上都是在解决“多个节点如何对同一件事达成一致”。

### 我读完后的直观感受
这一章比前面更抽象。作为 Java 开发，日常使用 Nacos 注册服务、OpenFeign 调接口、Redis 做缓存、MySQL 做主从，很少会主动想背后的共识问题。

比如平时我们看到：
- 服务下线后，Nacos 控制台显示实例不健康。
- OpenFeign 后续不再请求下线实例。
- 配置中心修改配置后，多个服务都能收到新配置。
- Redis 主节点挂了后，从节点被提升为新主。
- 定时任务集群中只有一个节点执行任务。

这些看起来都是中间件“自动完成”的事情。但背后其实都绕不开几个问题：
- 谁说这个节点已经下线？
- 所有节点是否都同意这个判断？
- 客户端什么时候能感知到最新结果？
- 如果网络分区，两边都觉得自己是对的怎么办？
- 如果两个节点都认为自己是主节点，谁说了算？
- 如果旧主恢复，它还能不能继续写？

一致性和共识的意义，就是在这些不可靠条件下，尽量让系统对关键事实形成一个可靠判断。

### 关键概念

#### 1. 一致性不是一个单一概念
日常开发中经常说“数据一致性”，但这个词其实有很多层含义。

可能指：
- 事务的一致性：业务约束没有被破坏。
- 副本一致性：多个副本看到的数据是否一样。
- 读写一致性：写入后能否立即读到。
- 缓存一致性：缓存和数据库是否一致。
- 配置一致性：所有服务是否使用同一份配置。
- 注册中心一致性：所有客户端看到的服务列表是否一致。
- 共识一致性：多个节点是否对某个决定达成一致。

所以讨论一致性时，不能只说“要强一致”或“最终一致”，必须先问：是哪类一致性？在哪个边界上？对谁可见？允许延迟多久？

对 Java 开发来说，一个系统可能同时存在多种一致性：
- 订单创建需要本地事务一致性。
- 商品列表缓存可以最终一致。
- 支付状态需要更强的读写一致性。
- 服务注册发现可以接受短暂延迟。
- 分布式锁和主节点选举需要更强的共识保证。

#### 2. 线性一致性
线性一致性是一种很强的一致性模型。它要求系统表现得像只有一个最新副本一样：一旦某个写入成功，之后所有读都应该看到这个写入或更新的结果。

可以理解为：所有操作都排成一个全局顺序，而且这个顺序要符合真实时间。先完成的写入，后开始的读取必须能看到它。

例如一个配置中心中，管理员把开关从 `false` 改成 `true`，如果系统提供线性一致性，那么修改成功后，之后所有读取配置的客户端都应该看到 `true`。

线性一致性的好处是非常符合直觉，开发者容易理解。缺点是成本高，尤其在跨机房、高延迟、网络分区场景下，要维持线性一致性会牺牲可用性或性能。

#### 3. 线性一致性和事务隔离不是一回事
这一点很容易混淆。

事务隔离讨论的是多个事务并发执行时，数据库内部如何隔离它们，比如可重复读、快照隔离、可串行化。

线性一致性讨论的是一个对象或寄存器在分布式副本之间的读写可见性，重点是外部观察到的读写顺序是否符合真实时间。

简单说：
- 可串行化关注事务之间能否等价于某个串行顺序。
- 线性一致性关注操作完成后的可见性是否像单副本一样。

一个数据库可以提供可串行化事务，但如果它是多副本异步复制，读从库时仍然可能不线性一致。反过来，一个单 key 的 KV 存储可能提供线性一致读写，但不一定支持复杂多行事务。

#### 4. 为什么线性一致性重要
很多分布式协调场景都依赖线性一致性。

例如分布式锁：
- A 获取锁成功。
- B 随后尝试获取同一把锁。
- 如果锁服务不是线性一致，B 可能在某些节点上看不到 A 已经持有锁，从而也获取成功。

例如领导者选举：
- 集群中只能有一个 leader。
- 如果多个节点基于不一致的视图做判断，就可能选出多个 leader。

例如唯一用户名注册：
- 用户名 `alice` 只能被一个用户注册。
- 如果不同副本并发接受写入，又没有强一致协调，就可能出现两个用户都注册成功。

这些场景的共同点是：系统必须对某个事实达成唯一判断。谁拿到了锁，谁是 leader，用户名是否被占用，配置版本谁最新，这些都需要更强的一致性。

#### 5. 共识是什么
共识的核心问题是：多个节点在可能发生故障、消息延迟、网络分区的情况下，对某个值达成一致。

这个值可以是：
- 谁是 leader。
- 某条日志是否提交。
- 某个配置版本是否生效。
- 某个节点是否被移出集群。
- 某次事务是否提交。

共识算法要保证几个性质：
- 所有正常节点最终决定同一个值。
- 已经决定的值不能被推翻。
- 决定的值必须来自某个合法提议。
- 只要多数节点正常，系统最终能继续前进。

这就是为什么共识通常需要多数派。多数派有一个关键性质：任意两个多数集合一定有交集。这个交集能把上一轮已经确认的信息传递到下一轮，防止两个互相隔离的小集群分别做出冲突决定。

#### 6. 多数派为什么能防脑裂
脑裂指的是网络分区后，多个节点或多个分区都认为自己是主，从而同时接受写入。

多数派机制的作用是：只有获得多数节点认可的一方，才有资格继续做决定。

例如 5 个节点的集群，至少需要 3 个节点同意才能选出 leader。如果网络分成 2 个节点和 3 个节点两边，只有 3 个节点那边能形成多数派，2 个节点那边不能继续选主。

这样可以避免两个分区都产生 leader。

这也是为什么很多分布式系统推荐奇数节点。3 节点可以容忍 1 个故障，5 节点可以容忍 2 个故障。2 节点集群很尴尬，因为只要断开，就没有一方能安全判断自己代表多数。

#### 7. 全序广播
全序广播指所有节点都以相同顺序接收和处理消息。

如果所有节点处理同一批命令，并且顺序完全一样，那么它们最终会得到相同状态。这就是复制状态机的思想。

例如：
- 命令 1：创建用户 A。
- 命令 2：修改用户 A 的状态。
- 命令 3：删除用户 A。

只要每个节点都按 1、2、3 的顺序执行，最终状态就一致。

Raft、Paxos、ZAB 等协议可以理解为在实现可靠的日志复制和顺序决定。它们让多个节点对“下一条日志是什么”达成一致，然后每个节点按相同顺序应用日志。

#### 8. 领导者选举
领导者选举是共识算法最常见的应用之一。

在很多系统里，为了避免并发冲突，会让一个 leader 负责接收写入或协调任务。问题是：当 leader 挂了，谁来接替？如何保证不会有两个 leader？

如果只是简单地说“谁先发现 leader 挂了谁就当 leader”，就很容易脑裂。正确做法通常要依赖多数派确认、任期编号、日志新旧比较等机制。

Raft 中有 term 的概念，可以理解为任期。每次选举进入新任期，节点只能投票给一个候选者。通过多数派投票，保证同一任期最多只有一个 leader。

这对 Java 开发的启发是：不要随便用数据库字段、Redis key 或本地判断就做“我是主节点”的逻辑。只要涉及集群选主，最好依赖成熟组件。

#### 9. fencing token
分布式锁里一个很重要但容易被忽略的概念是 fencing token，可以理解为栅栏令牌。

问题是：客户端 A 拿到了锁，但因为 GC 暂停、网络卡顿或业务执行过久，锁过期了。客户端 B 又拿到了新锁。如果 A 恢复后继续写数据，就可能破坏 B 的结果。

仅仅依赖“锁是否还在”不够，因为 A 可能以为自己仍然有权执行。

fencing token 的思路是：每次成功获取锁时，锁服务返回一个递增编号。下游资源只接受编号更大的请求，拒绝旧编号请求。

例如：
- A 获取锁，token = 10。
- A 暂停很久。
- 锁过期，B 获取锁，token = 11。
- B 写入数据库成功。
- A 恢复后携带 token = 10 继续写。
- 数据库发现 token 小于当前版本，拒绝 A 的写入。

这个思想很重要：分布式锁本身不能完全防止旧持有者继续执行，真正安全的系统还需要资源层校验。

#### 10. 2PC 和共识的关系
两阶段提交和共识都在协调多个节点，但它们解决的问题不同。

2PC 解决的是一个事务在多个参与者之间要不要提交。协调者问所有参与者是否准备好，如果都准备好，就通知提交，否则回滚。

共识解决的是多个节点如何对某个值达成一致，并且在部分节点故障时仍能继续安全前进。

2PC 的问题是协调者故障时可能阻塞。参与者进入 prepared 状态后，需要等待协调者最终决定。如果协调者挂了，参与者不知道该提交还是回滚。

共识算法通常可以通过多数派继续选出新的 leader，继续推进日志提交。因此很多现代系统会用共识日志来实现更可靠的协调。

#### 11. Nacos 中的一致性思考
作为 Java 开发，Nacos 是最常接触的一致性组件之一。

Nacos 既做服务注册发现，也做配置管理。不同数据的要求不同：
- 服务实例临时上下线，更看重可用性和快速感知，通常可以接受短暂最终一致。
- 配置数据、持久化数据，更看重一致性，不能多个节点长期看到不同配置。

根据 Nacos 官方文档，Nacos 集群中会使用 Distro 和 Raft/JRaft 这类协议来处理不同类型数据的一致性。Distro 更偏 AP 和最终一致，常用于临时服务实例；Raft/JRaft 更偏 CP 和强一致，常用于需要更强一致保障的数据。

所以当一个服务下线时，Nacos 控制台提示不健康，OpenFeign 后续不再访问下线实例，这背后通常包括：
- 服务实例向 Nacos 发送心跳。
- Nacos 根据心跳判断实例健康状态。
- Nacos 节点之间同步实例变更。
- 客户端订阅或拉取最新服务列表。
- Spring Cloud LoadBalancer 或 Ribbon 更新本地实例缓存。
- OpenFeign 发起调用时从可用实例列表中选择节点。

这里要注意：这不是瞬间完成的强一致过程。服务下线到所有客户端都不再请求它，中间可能存在一个很短的延迟窗口。

因此 Java 开发不能假设“服务一下线，所有请求立刻都不会打过去”。真实系统中仍然可能出现：
- 客户端本地服务列表还没刷新。
- 已经建立的连接还在发送请求。
- 网关或负载均衡缓存还没更新。
- Nacos 节点之间同步有延迟。
- 服务进程还没完全停止，但已经不健康。

这也是为什么服务下线要配合优雅停机，而不是直接 kill 进程。

#### 12. Nacos 与 OpenFeign 的实践理解
OpenFeign 本身只是声明式 HTTP/RPC 调用客户端，它并不直接保证一致性。它依赖注册中心和负载均衡组件提供可用实例列表。

大致链路是：
- 服务提供者注册到 Nacos。
- 消费者从 Nacos 获取服务实例列表。
- OpenFeign 发起调用。
- 负载均衡器选择一个实例。
- 实例不健康或下线后，注册中心通知或客户端刷新列表。
- 后续请求避开下线实例。

这说明服务发现的一致性是一个链路问题，不只是 Nacos 服务端的问题。

实践上要注意：
- 服务关闭前先停止接收新流量。
- 给应用配置优雅停机时间。
- 健康检查失败后不要立即退出，要给注册中心传播时间。
- Feign 要配置合理超时、重试和熔断。
- 被调用方接口要幂等，防止重试造成重复写入。
- 核心服务下线要观察调用错误率，而不是只看 Nacos 控制台。

#### 13. Redis 集群中的一致性和脑裂
Redis 相关系统也涉及一致性和故障切换。

Redis Sentinel 会通过多个 Sentinel 节点监控主节点，并在判断主节点不可用时发起故障转移。为了避免多个 Sentinel 各自做不同决定，它需要 quorum、多数派授权和配置纪元等机制。

Redis Cluster 也有自己的集群协议。它不是用 Raft 来复制所有数据，而是通过 gossip 传播集群状态，通过 `currentEpoch`、`configEpoch`、多数派授权等机制处理故障转移和槽位归属冲突。Redis 官方文档中也提到，副本故障转移时需要从多数 master 获得新的配置纪元。

所以你听说 Redis 集群用一些机制防止脑裂，这个方向是对的，但要更准确地理解：Redis Cluster 的目标不是提供像 Raft 日志那样的强一致复制，而是在主从异步复制和分片架构下，通过多数派、epoch、故障检测和配置传播，尽量避免多个节点同时声称负责同一批槽位。

另外，Redis 主从复制通常是异步的。即使故障切换成功，也可能丢失旧主上尚未复制到从节点的数据。

这对 Java 开发的启发是：
- Redis 高可用不等于强一致。
- Redis 锁和缓存不能承载无法丢失的核心状态。
- Redis failover 期间要允许短暂错误和重试。
- 重要业务状态仍然应该以数据库或更强一致的存储为准。

#### 14. ZooKeeper、etcd、Consul 这类协调组件
很多协调组件都建立在共识算法之上。

ZooKeeper 使用 ZAB 协议，etcd 使用 Raft，Consul 也使用 Raft。它们通常提供：
- 强一致的元数据存储。
- 临时节点或租约。
- 监听机制。
- leader 选举。
- 分布式锁。
- 配置管理。
- 服务发现。

Java 开发不一定要实现这些协议，但要知道什么时候应该使用这类组件：
- 需要全局唯一 leader。
- 需要可靠的配置版本。
- 需要集群成员关系一致。
- 需要任务只被一个节点执行。
- 需要分布式锁，并且锁结果必须尽量可靠。

不要用普通缓存、数据库轮询或本地变量随便实现复杂协调逻辑。

#### 15. 共识解决不了所有问题
共识算法很强，但不是银弹。

它不能解决：
- 业务设计错误。
- 客户端不幂等。
- 下游接口重复执行。
- 网络延迟导致的用户体验问题。
- 跨地域强一致带来的高延迟。
- 数据已经被错误业务逻辑写坏。
- 所有节点同时故障或磁盘损坏。

共识更多是提供一个可靠的顺序和决定机制。业务仍然要设计状态机、幂等、补偿、监控和降级。

### Java 开发什么时候应该关心一致性和共识

#### 1. 使用注册中心时
当使用 Nacos、Eureka、Consul 这类注册中心时，要关心服务列表不是瞬间一致的。

服务下线后，客户端可能仍然在短时间内访问旧实例。因此服务发布和下线要做优雅停机，调用方要有超时、重试和熔断，被调用方接口要能承受重复调用。

#### 2. 使用配置中心时
配置中心发布配置后，多个服务实例可能不是同一毫秒同时生效。

如果一个配置变更会影响业务规则，例如价格策略、风控开关、灰度比例，就要考虑新旧配置短暂共存的问题。

必要时可以设计配置版本号、灰度发布、回滚策略和兼容逻辑。

#### 3. 使用分布式锁时
分布式锁背后依赖一致性。如果锁服务本身不可靠，或者锁过期后旧持有者还能继续写，业务就可能出错。

关键业务不要只靠锁，要配合：
- fencing token。
- 数据库版本号。
- 状态条件更新。
- 唯一索引。
- 幂等表。

#### 4. 做集群定时任务时
多实例部署后，定时任务只执行一次其实就是 leader 选举或分布式协调问题。

例如每天关单、补偿退款、生成报表，如果多个节点同时跑，就可能重复处理。可以使用 XXL-JOB、Quartz 集群、ShedLock、ZooKeeper、数据库唯一任务记录等方式，但业务更新仍然要幂等。

#### 5. 做主从切换和高可用时
MySQL、Redis、Kafka、Nacos、ElasticSearch 等组件都有故障切换问题。

Java 应用要关心：
- 切换期间是否会有短暂不可用。
- 客户端连接是否会自动恢复。
- 是否会读到旧主或旧副本。
- 是否可能出现重复提交。
- 是否需要重试和幂等。

#### 6. 做唯一性和全局顺序时
如果业务需要全局唯一用户名、全局递增订单号、全局唯一任务执行权，就要关心一致性。

不要只靠本地缓存判断用户名是否存在。最终兜底通常应该是数据库唯一索引、强一致存储或共识组件。

#### 7. 做多机房系统时
多机房系统天然会遇到网络分区和高延迟问题。

如果两地都能写，就要考虑冲突解决。如果只有一个主写，就要考虑主机房故障切换。如果要求强一致，就要接受跨地域延迟。

一致性不是免费得到的，跨地域系统尤其要明确 CP 和 AP 取舍。

### 架构中的主要权衡

#### 一致性 vs 可用性
网络分区时，系统通常不能同时保证强一致和完全可用。强一致系统可能拒绝少数派请求；高可用系统可能接受短暂不一致。

注册中心临时实例更偏可用性，配置中心关键配置更偏一致性。不同数据类型应该选择不同策略。

#### 强协调 vs 高性能
共识算法需要节点通信和多数派确认，性能成本比单机写入高。不是所有数据都需要共识。用户浏览记录、日志、推荐特征可以最终一致；锁、leader、配置版本、集群元数据更适合强协调。

#### 自动恢复 vs 错误切换
自动故障切换能提升可用性，但如果故障检测误判，可能引发脑裂或错误主从切换。自动化越强，越需要多数派、epoch、租约、fencing token 等保护机制。

#### 客户端简单 vs 系统复杂
线性一致系统让客户端逻辑更简单，但中间件实现复杂、延迟更高。最终一致系统中间件更可用，但客户端和业务代码要处理重试、补偿、幂等、版本冲突。

#### 本地判断 vs 统一事实来源
本地缓存和本地判断速度快，但容易过期。强一致存储或注册中心可以作为统一事实来源，但访问成本更高。关键决策要依赖可靠事实来源，普通展示可以用缓存。

### 对日常 Java 开发的实际帮助

#### 1. 理解 Nacos 下线不是瞬间全局生效
Nacos 控制台看到实例下线，不代表所有调用方立刻不会访问它。客户端本地缓存、连接池、推送延迟、负载均衡刷新都会造成短暂窗口。

所以服务发布要做优雅停机，接口要有超时，调用方要有容错。

#### 2. 不要把 OpenFeign 当成可靠本地调用
OpenFeign 只是远程调用封装。注册中心和负载均衡可以减少访问下线节点，但不能保证没有超时、没有半成功、没有重复请求。

写接口必须幂等，读接口要能降级，核心链路要有熔断和限流。

#### 3. Redis 高可用不是强一致
Redis Sentinel 和 Redis Cluster 可以自动故障切换，但 Redis 复制通常是异步的。failover 后可能有短暂不可用，也可能丢失少量未复制数据。

因此 Redis 适合缓存、限流、临时状态、排行榜等场景，不适合作为唯一可靠账本。

#### 4. 分布式锁要配合资源层校验
锁服务告诉你“拿到锁”只是协调结果，不能保证业务执行期间永远安全。GC 暂停、锁过期、网络分区都可能破坏假设。

关键写入要加版本号、状态条件或 fencing token。

#### 5. 配置发布要考虑新旧版本共存
配置中心变更后，不同服务实例可能短时间使用不同配置。配置结构和含义要兼容，不能让新配置一发布就导致旧代码崩溃。

这和前面“编码与演化”的思想是一样的：新旧版本会共存。

#### 6. 把唯一性留给可靠机制兜底
注册用户名、创建唯一业务单、领取唯一权益，不要只用缓存判断。缓存可以加速，数据库唯一索引或强一致组件才是最终兜底。

#### 7. 学会区分数据类型
不是所有数据都需要强一致。

可以按重要性区分：
- 强一致：锁、leader、配置版本、支付状态、余额。
- 较强一致：库存、订单状态、权限变更。
- 最终一致：缓存、搜索索引、消息通知、报表、推荐。

不同数据选择不同一致性模型，系统才不会过度复杂。

### 实践举例

#### 1. 服务下线与 OpenFeign 调用
服务 A 调用服务 B。服务 B 的一个实例准备下线。

如果直接 kill：
- Nacos 需要时间感知实例不健康。
- 服务 A 本地可能还缓存着旧实例。
- OpenFeign 可能仍然把请求打过去。
- 用户请求可能失败。

更好的做法：
- 服务 B 先标记不接收新流量。
- 等待注册中心和客户端刷新。
- 继续处理已进入的请求。
- 完成后再退出进程。
- 服务 A 配置超时、重试、熔断。

这说明注册中心提供的是服务发现能力，不是瞬时强一致屏障。

#### 2. 配置中心灰度发布
风控服务从 Nacos 读取一个开关：

```text
risk.check.enabled=true
```

如果直接全量修改，部分实例可能已经读到新配置，部分实例还没刷新。短时间内同一类请求可能走不同逻辑。

更好的做法：
- 配置加版本号。
- 支持灰度范围。
- 新旧配置结构兼容。
- 保留快速回滚能力。
- 关键规则变更要观察指标。

#### 3. Redis 锁和 fencing token
一个定时任务用 Redis 锁保证单实例执行。

问题是锁 30 秒过期，但任务执行 60 秒。第二个实例拿到新锁后开始执行，第一个实例恢复后也继续写。

更好的做法：
- 获取锁时生成递增 token。
- 写数据库时带 token 或版本校验。
- 数据库拒绝旧 token 的写入。
- 任务本身保持幂等。

锁负责协调，数据库负责最终保护。

#### 4. Redis 主从切换
Redis 主节点写入一个 key 后立刻宕机，这个写入还没复制到从节点。从节点被提升为新主。

结果可能是：
- Java 客户端重连成功。
- Redis 集群恢复可用。
- 但刚才写入的 key 丢失。

所以如果这个 key 是缓存，问题不大；如果它是唯一订单状态，就很危险。

这说明 Redis 高可用和数据强一致是两回事。

#### 5. 用户名唯一注册
两个请求同时注册同一个用户名。

错误做法：
- 先查缓存中用户名不存在。
- 再插入用户表。

并发下两个请求都可能查到不存在。

更好的做法：
- 用户名字段建唯一索引。
- 插入失败时返回用户名已存在。
- 缓存只做加速，不做最终判断。

唯一性问题应该交给强约束兜底。

#### 6. 集群任务选主
一个报表任务每天只能执行一次。服务部署了 5 个实例。

可选方案：
- 使用调度平台保证单实例调度。
- 使用数据库任务表和唯一约束抢占任务。
- 使用 ZooKeeper/etcd/Nacos 选主。
- 使用分布式锁，但任务处理要幂等。

核心思想是：执行权必须有一个可靠的统一判断，不能每个节点靠本地时间自己决定。

### 我的理解
- 一致性与共识讨论的不是普通业务代码怎么写，而是分布式系统如何对关键事实达成一致。
- Java 开发虽然不常实现共识算法，但每天都在使用依赖共识或一致性协议的组件。
- Nacos 服务发现让 OpenFeign 避开下线实例，但这个过程不是瞬时强一致的，中间存在传播延迟和本地缓存窗口。
- Nacos 中不同数据有不同一致性策略，临时服务实例更偏 AP 和最终一致，配置或持久化数据更偏 CP 和强一致。
- Redis Cluster 和 Sentinel 会用多数派、epoch、故障检测等机制降低脑裂风险，但 Redis 数据复制本身通常不是强一致。
- 分布式锁不是最终正确性的保证，关键写入仍然需要 fencing token、版本号、状态机或数据库约束兜底。
- 共识算法的价值是提供可靠顺序和唯一决定，但业务正确性仍然要靠幂等、状态机、补偿和监控。
- 一致性不是越强越好，强一致有成本；真正成熟的系统会按数据重要性选择不同一致性级别。

### 可落地的实践启发
- 服务下线要做优雅停机，不要直接 kill 进程。
- OpenFeign 调用必须配置超时、重试、熔断和降级。
- 写接口必须幂等，因为注册发现和重试都不能保证只调用一次。
- 不要假设 Nacos 服务列表瞬间全局一致。
- 配置中心发布要考虑新旧配置短暂共存。
- Redis 高可用不等于 Redis 强一致，核心账本不要只放 Redis。
- 分布式锁要配合数据库版本号、状态条件或 fencing token。
- 集群定时任务必须有可靠选主或抢占机制。
- 唯一性约束要靠数据库唯一索引或强一致存储兜底。
- 多节点选主、分布式锁、配置版本、集群元数据优先使用成熟协调组件。
- 强一致只用在真正需要的地方，普通缓存、搜索索引、通知可以最终一致。
- 设计系统时要明确：哪些数据必须大家马上看到一致，哪些数据可以延迟一致。

### 一句话总结
一致性与共识的核心，是在节点会故障、网络会分区、客户端会缓存旧数据的情况下，让系统对关键事实形成唯一可靠的判断；对 Java 开发来说，理解它不是为了手写 Raft，而是为了正确使用 Nacos、Redis、OpenFeign、分布式锁、配置中心和高可用组件，并在业务代码里补上幂等、状态机、优雅停机和资源层兜底。
