# 《设计数据密集型应用》读书笔记

## 第 6 章：复制

### 本章核心
这一章讲的是数据复制，也就是把同一份数据保存到多台机器上，让系统获得更高的可用性、读性能和容错能力。对 Java 后端开发来说，最常接触的是 MySQL 的主从复制，也就是书中说的单主复制模型。但这一章真正重要的不是记住几种复制架构的名字，而是理解复制会带来的延迟、一致性、故障切换和并发写入问题。

### 我读完后的直观感受
这一章相对好理解，因为主从复制、读写分离、从库延迟这些问题在 Java 业务开发中比较常见。平时我们可能只是知道“写主库、读从库”，或者在配置里接入了动态数据源，但很少深入思考：为什么读从库会读不到刚写入的数据？主库挂了以后到底会发生什么？自动切换为什么可能导致数据丢失？

书里讲的单主复制、多主复制、无主复制，看起来是数据库底层架构，但它们其实会直接影响业务代码的正确性。尤其是当系统规模变大、跨机房部署、读写压力上升之后，复制不只是 DBA 或中间件的问题，后端开发也必须理解它的行为边界。

### 关键概念

#### 1. 为什么需要复制
复制的核心目标是把同一份数据保存在多个节点上。

主要原因包括：
- 提高可用性：一台机器故障时，其他副本仍然可以提供服务。
- 提高读性能：读请求可以分散到多个副本上。
- 降低访问延迟：数据可以放在离用户更近的区域。
- 增强容灾能力：避免单点故障导致数据完全不可用。

从 Java 后端视角看，最常见的场景是数据库读写分离：写请求进入主库，读请求分发到一个或多个从库。这样可以缓解主库读压力，但也引入了主从延迟和一致性问题。

#### 2. 单主复制
单主复制是最常见的复制模型。系统中只有一个主节点可以接受写入，其他从节点从主节点复制数据。

典型结构是：
- 应用写入主库。
- 主库记录变更日志。
- 从库拉取或接收主库日志。
- 从库按顺序重放日志，得到和主库相同的数据。
- 应用可以从从库读取数据。

MySQL 主从复制就是典型的单主复制。Java 业务中常见配置是：
- `insert`、`update`、`delete` 走主库。
- 普通查询走从库。
- 强一致查询走主库。

这种模型的好处是写入冲突少，因为所有写入都经过一个主库。缺点是主库成为写入瓶颈，而且主库故障时需要进行故障切换。

#### 3. 同步复制与异步复制
复制可以分为同步复制和异步复制。

同步复制指主库写入数据后，必须等从库也确认写入成功，才向客户端返回成功。优点是数据更安全，缺点是延迟高，并且从库故障会影响主库写入。

异步复制指主库写入成功后立即返回，从库稍后再复制。优点是性能好、延迟低，缺点是主库故障时，尚未复制到从库的数据可能丢失。

实际系统里，完全同步复制成本较高，所以常见的是半同步或异步复制。MySQL 主从复制在很多业务场景中使用异步复制，这也是为什么主库宕机后可能出现少量数据丢失。

#### 4. 主从延迟
主从延迟是单主复制中最常见的问题。应用写入主库后，从库不是立即拥有这条数据，而是需要等待复制日志传输和重放。

典型问题是：用户刚提交订单，马上查询订单列表，却查不到刚才的订单。

伪代码类似：

```java
orderService.createOrder(command); // 写主库
List<OrderVO> orders = orderQueryService.listMyOrders(userId); // 读从库
```

如果查询走从库，而从库还没复制到最新数据，就会出现“写后读不到”的问题。

常见解决方式包括：
- 写后短时间内读主库。
- 对强一致页面强制读主库。
- 根据用户维度做会话一致性控制。
- 判断从库延迟，延迟过高时降级读主库。
- 关键流程不要依赖刚写完后立刻读从库。

这说明读写分离不是简单地把所有 `select` 都打到从库，而是要结合业务一致性要求做路由。

#### 5. 读己之写
读己之写指用户写入数据后，自己随后读取时应该能看到刚才写入的结果。

例如用户修改昵称后，刷新个人资料页，应该看到新昵称。如果因为读从库延迟导致仍然显示旧昵称，用户会认为系统没有保存成功。

在 Java Web 系统中，可以用一些实践方式保证读己之写：
- 修改个人资料后的一段时间内，该用户请求读主库。
- 写入成功后直接返回最新数据，避免马上再次查询。
- 将最近写入时间记录在 session、token 或上下文中，用于动态路由数据源。
- 对个人中心、订单详情、支付结果等页面优先读主库。

这里的重点是：不是所有读都需要强一致，但用户刚刚操作过的数据通常需要更强的一致性。

#### 6. 单调读
单调读指用户多次读取时，不应该先看到新数据，后看到旧数据。

如果用户第一次请求被路由到复制较快的从库，第二次请求被路由到复制较慢的从库，就可能出现页面“倒退”的感觉。

例如第一次看到订单状态是“已支付”，刷新后却变成“待支付”。这不是业务回滚，而是不同从库复制进度不同。

常见解决方式包括：
- 同一个用户固定路由到同一个从库。
- 按用户 ID 做一致性哈希。
- 从库延迟过高时不参与读请求。
- 对状态敏感的数据读主库。

这类问题平时不一定高频出现，但一旦出现，非常影响用户信任。

#### 7. 一致前缀读
一致前缀读指如果一系列写入有因果顺序，读取时也应该按照这个顺序看到。

例如聊天系统中：
- A 先发送：“你看到我的消息了吗？”
- B 后回复：“看到了。”

如果复制延迟或分区导致用户只看到 B 的回复，却没看到 A 的问题，就会出现因果顺序错乱。

在普通 Java 业务系统里，这类问题可能出现在：
- 订单状态流水。
- 审批流记录。
- 聊天消息。
- 评论与回复。
- 操作日志。

解决思路通常是把有因果关系的数据放到同一个分区或同一个复制链路中，或者查询时按主库、版本号、时间戳和序列号保证顺序。

#### 8. 故障切换
单主复制中，如果主库故障，需要选择一个从库提升为新的主库，这个过程叫故障切换。

故障切换通常包括：
- 判断主库是否真的故障。
- 选择一个最新的从库作为新主库。
- 让其他从库改为复制新主库。
- 应用连接切换到新主库。
- 处理旧主库恢复后的数据冲突。

这里最难的是“判断主库是否真的故障”。如果只是网络抖动，主库其实还活着，但系统错误地选出了新主库，就可能出现两个主库同时接受写入，也就是脑裂。

故障切换还可能导致数据丢失。因为异步复制下，主库已经返回成功的数据，可能还没复制到从库。如果此时主库宕机，新主库上就没有这部分数据。

对业务开发来说，这意味着：即使数据库有高可用，也不代表业务永远不会遇到重复、丢失、乱序和短暂不可用。

#### 9. 多主复制
多主复制允许多个节点都接受写入，然后彼此同步数据。它适合多机房、多区域场景，因为每个区域都可以写本地节点，降低跨地域写入延迟。

但多主复制最大的问题是写入冲突。

例如同一个用户资料在两个机房被同时修改：
- 北京节点把昵称改成 `Alice`。
- 上海节点把昵称改成 `Bob`。

两个写入同步后，系统必须决定最终保留哪个值。

冲突解决方式可能包括：
- 最后写入获胜。
- 按版本号合并。
- 按字段合并。
- 人工处理冲突。
- 业务上避免同一份数据多点写入。

对大多数 Java 业务系统来说，多主复制不如单主复制常见，因为它把复杂度从数据库复制层传递到了业务一致性层。除非确实有跨地域低延迟写入需求，否则不要轻易引入。

#### 10. 无主复制
无主复制没有固定主节点，客户端可以向多个副本读写。Dynamo 风格系统、部分分布式 KV 存储会使用这种模型。

它通常会用读写 quorum 来保证一定程度的一致性。例如有 3 个副本，写入至少成功 2 个，读取至少读取 2 个，只要读写集合有交集，就有机会读到最新值。

这种模型的优势是可用性高，单个节点故障不影响整体写入。缺点是客户端和存储系统需要处理版本冲突、读修复、反熵同步和最终一致性。

在 Java 开发中，虽然直接实现无主复制的机会不多，但使用 Cassandra、DynamoDB、Riak 或一些分布式 KV 系统时，会遇到类似思想。

### 架构中的主要权衡

#### 可用性 vs 一致性
复制可以提高可用性，但副本之间同步需要时间。系统越希望故障时继续提供服务，就越可能接受短时间数据不一致。读写分离就是典型例子：读性能和可用性提高了，但读到旧数据的概率也增加了。

#### 读性能 vs 数据新鲜度
读从库可以分摊主库压力，但从库数据可能落后于主库。读请求越多地走从库，主库压力越小，但业务越需要考虑主从延迟。订单支付、余额、库存这类场景通常不能简单读从库。

#### 自动故障切换 vs 数据安全
自动故障切换可以缩短故障恢复时间，但如果判断错误或复制延迟较大，可能导致脑裂或数据丢失。故障切换不是越快越好，而是要在恢复速度和数据正确性之间取舍。

#### 单主简单性 vs 多主灵活性
单主复制写入路径简单，冲突少，适合大多数业务系统。多主复制可以支持多区域写入，但冲突处理复杂。除非业务真的需要多点写入，否则单主模型通常更容易维护。

#### 强一致体验 vs 系统吞吐
如果所有读都走主库，用户体验更一致，但主库压力会更大。如果大量读走从库，系统吞吐更高，但要处理读己之写、单调读、一致前缀读等问题。

### 对日常 Java 开发的实际帮助

#### 1. 更理解读写分离不是简单规则
很多项目会配置动态数据源，让写操作走主库，读操作走从库。但真实业务里，不能粗暴地认为所有 `select` 都可以读从库。

例如：
- 用户刚下单后查询订单详情，应该读主库。
- 支付成功后查询支付状态，应该读主库。
- 后台报表、商品列表、历史记录，可以读从库。
- 对一致性不敏感的列表页，可以读从库。

所以数据源路由要根据业务场景，而不是只根据 SQL 类型。

#### 2. 更谨慎处理主从延迟
主从延迟不是数据库异常，而是复制模型的正常现象。业务代码要默认它可能存在。

实践中可以：
- 写入后直接返回需要展示的数据。
- 写入后短时间内标记当前用户读主库。
- 对关键查询提供强制读主库选项。
- 监控 `Seconds_Behind_Master` 或复制延迟指标。
- 从库延迟过高时暂停流量切入。

#### 3. 更关注幂等和补偿
主库故障、故障切换、网络超时都可能让应用不知道一次写入到底有没有成功。客户端重试时，就可能产生重复请求。

因此 Java 业务接口要重视：
- 幂等键。
- 唯一索引。
- 状态机约束。
- 请求流水号。
- 重试安全性。
- 补偿任务。

例如创建订单接口不能只依赖“前端不要重复点”，而应该用业务单号或幂等表防止重复下单。

#### 4. 更理解缓存一致性问题
数据库复制延迟和缓存延迟叠加后，业务看到的数据可能更复杂。

例如写主库后删除缓存，但读请求打到从库，从库还是旧数据，于是旧数据又被写回缓存。这就是常见的缓存和主从复制组合问题。

解决方式可能包括：
- 更新后短时间读主库。
- 延迟双删。
- 缓存设置较短过期时间。
- 对关键数据避免读从库回填缓存。
- 使用消息或 binlog 做缓存失效。

这说明缓存一致性不能只看缓存本身，还要结合数据库复制延迟一起分析。

#### 5. 更能判断哪些数据适合读从库
适合读从库的数据通常是：
- 商品详情。
- 新闻内容。
- 配置展示。
- 历史订单列表。
- 日志查询。
- 报表统计。

不适合随意读从库的数据通常是：
- 余额。
- 库存扣减结果。
- 支付状态。
- 刚创建的订单。
- 权限变更。
- 用户刚修改的个人资料。

判断标准不是“这是不是查询”，而是“用户是否要求立刻看到最新写入”。

#### 6. 更谨慎看待数据库高可用
数据库高可用不是魔法。它可以减少宕机时间，但不能消除所有一致性问题。

业务上仍然要考虑：
- 写入是否可能丢失。
- 请求是否可能重复。
- 状态是否可能短暂回退。
- 切换期间是否有短暂不可用。
- 老主库恢复后如何处理。

所以后端开发不能只说“数据库有主从和高可用就行了”，还要理解高可用背后的边界。

### 实践举例

#### 1. 用户修改昵称
用户修改昵称后，如果接口写主库，页面刷新却读从库，就可能仍然显示旧昵称。

更好的做法：
- 修改接口直接返回最新昵称。
- 修改后几秒内该用户读主库。
- 个人资料页优先读主库。

这里追求的是读己之写，而不是所有用户都必须马上看到最新昵称。

#### 2. 订单创建后查询详情
下单接口写入主库后，前端马上跳转订单详情页。如果订单详情读从库，可能查不到订单。

更好的做法：
- 下单成功响应中返回订单核心信息。
- 订单详情首屏读主库。
- 订单列表可以根据场景读从库。
- 如果查不到订单，不要马上判断订单不存在，可以重试或读主库兜底。

#### 3. 支付状态查询
支付状态对用户和业务都很敏感。如果支付成功后仍然显示未支付，用户可能重复支付或联系客服。

更好的做法：
- 支付回调更新主库。
- 支付结果页读主库。
- 状态流转使用明确状态机。
- 支付回调和主动查询都要幂等。

这类场景不适合为了读性能牺牲一致性体验。

#### 4. 商品列表读取
商品列表通常可以接受短时间延迟。商家刚修改商品描述，用户晚几秒看到通常问题不大。

更好的做法：
- 商品详情和列表可以读从库或缓存。
- 后台编辑成功后，商家自己的预览可以读主库。
- 对外展示允许最终一致。

这体现了同一份数据在不同场景下可以有不同一致性要求。

#### 5. 缓存回填旧数据
写入主库并删除缓存后，如果紧接着有读请求读从库，由于从库还没同步新数据，读到旧值后又写入缓存，就会造成旧缓存复活。

更好的做法：
- 更新后短时间读主库。
- 延迟再次删除缓存。
- 回填缓存时检查数据版本。
- 关键数据使用更严格的一致性策略。

这个例子说明复制延迟会放大缓存一致性问题。

### 我的理解
- 复制不是简单地多存几份数据，而是在可用性、性能和一致性之间做取舍。
- 单主复制是 Java 业务系统最常见的模型，它简单、实用，但必须接受主从延迟和故障切换问题。
- 读写分离不是所有写走主库、所有读走从库这么简单，关键要看业务是否需要读到最新数据。
- 从库延迟是复制模型的正常现象，不应该只在出问题时才意识到它存在。
- 故障切换可以提升可用性，但也可能带来数据丢失、重复写入、脑裂和短暂不可用。
- 多主复制和无主复制提高了写入灵活性或可用性，但会把冲突解决和一致性问题变得更复杂。
- 后端开发虽然不一定维护数据库复制链路，但必须理解复制对业务正确性的影响。

### 可落地的实践启发
- 读写分离要按业务场景路由，不要简单按 SQL 类型路由。
- 用户刚写入的数据，后续查询优先读主库。
- 订单、支付、余额、库存、权限等强一致场景慎用从库读取。
- 商品、内容、日志、报表等允许延迟的场景可以读从库。
- 监控主从延迟，并在延迟过高时自动摘除从库或切换读主库。
- 写接口要设计幂等，避免故障切换或超时重试造成重复数据。
- 缓存一致性方案要同时考虑数据库主从延迟。
- 故障切换期间要允许短暂失败，并设计重试、降级和补偿机制。
- 不要轻易引入多主复制，除非业务确实需要多地域写入。
- 对有因果顺序的数据，例如聊天、审批、状态流水，要特别注意读取顺序。
- 数据库高可用不是业务正确性的全部保障，业务代码仍然要处理边界情况。

### 一句话总结
复制的核心不是“主从同步”这么简单，而是在多副本环境下平衡可用性、性能和一致性；对 Java 后端开发来说，最重要的是理解主从延迟、读写分离、故障切换和幂等补偿如何影响真实业务行为。
