# 《设计数据密集型应用》读书笔记

## 第 9 章：分布式系统的麻烦

### 本章核心
这一章讲的是分布式系统中那些看起来“不归业务开发管”，但最终会影响业务正确性的麻烦：网络不可靠、节点可能宕机、进程可能暂停、时钟可能不准、超时不一定代表失败、重试可能造成重复、故障检测可能误判。

对 Java 后端开发来说，平时确实很少直接关心网线、交换机、磁盘、CPU、时钟漂移这些底层问题。大部分时候，我们只是在写 `Controller`、`Service`、`Mapper`，调用数据库、Redis、MQ、RPC 接口。但一旦系统跨进程、跨机器、跨服务，这些底层不确定性就会通过超时、重复请求、数据不一致、锁失效、任务重复执行等形式暴露到业务代码里。

所以这一章的重点不是让每个 Java 开发都变成网络工程师，而是要建立一个基本认知：分布式系统中没有绝对可靠的远程调用，也没有绝对准确的故障判断。业务代码必须默认失败、延迟、重复和乱序都会发生。

### 我读完后的直观感受
这一章读起来可能会比较抽象，因为很多内容离日常 CRUD 比较远。比如网络分区、时钟漂移、进程暂停、故障检测，平时开发业务功能时不一定会主动考虑。

但仔细想会发现，Java 开发其实经常在间接面对这些问题：
- 调用第三方支付接口超时了，但对方到底有没有扣款成功？
- MQ 消息重复消费了，业务会不会重复发券、重复加积分？
- Redis 分布式锁设置了 30 秒过期，但业务执行了 60 秒会怎样？
- 定时任务在两台机器上同时跑了，会不会重复处理数据？
- 服务注册中心把某个实例判定下线，但它是真的死了，还是网络抖动？
- 数据库主从切换时，应用会不会写到旧主库？
- 依赖服务突然变慢，线程池被打满后会不会拖垮自己？

这些都不是纯业务逻辑问题，而是分布式系统不可靠性在业务层的表现。

### 关键概念

#### 1. 分布式系统和单机系统最大的区别
单机程序里，很多失败是明确的。比如方法调用失败会抛异常，进程崩溃后程序停止，内存中的状态基本由当前进程控制。

分布式系统不一样。只要一次调用跨过网络，就会出现很多不确定状态：
- 请求可能没有发出去。
- 请求可能发出去了，但对方没收到。
- 对方可能收到了，也处理成功了，但响应丢了。
- 对方可能处理了一半崩溃了。
- 响应可能很久之后才回来。
- 客户端超时了，但服务端仍在继续执行。

所以在分布式系统中，超时不等于失败，失败也不一定意味着对方没有执行。

这对 Java 开发非常重要，因为我们经常把远程调用写得像本地方法：

```java
paymentClient.pay(orderId, amount);
```

代码看起来像普通方法调用，但它背后可能经过 HTTP、RPC、负载均衡、网关、网络、对方服务、数据库、消息队列。任何一层都可能失败或变慢。

#### 2. 网络是不可靠的
网络请求可能出现延迟、丢包、抖动、连接中断、DNS 问题、路由异常、负载均衡切换等情况。

对应用来说，最常见的表现是：
- 请求超时。
- 连接被拒绝。
- 连接重置。
- 偶发 502、503、504。
- RPC 调用卡住。
- 响应时间突然升高。

Java 开发什么时候需要关心网络问题？

第一，所有远程调用都要设置超时时间。不能让 HTTP、RPC、数据库、Redis 调用无限等待。

第二，调用外部系统时要考虑重试和幂等。比如支付、发券、创建订单这类操作，不能简单地超时后立刻重试，否则可能重复执行。

第三，系统要有降级和熔断。依赖服务慢，不应该把自己的 Tomcat 线程池、业务线程池、数据库连接池全部拖死。

#### 3. 超时不能证明对方失败
超时只说明调用方在规定时间内没有收到响应，不代表服务端没有执行成功。

例如用户支付时：

```java
PayResult result = paymentClient.pay(request);
```

如果这次调用超时，可能有几种情况：
- 支付请求根本没到支付系统。
- 支付系统收到了，但还没处理。
- 支付系统处理成功了，但响应丢了。
- 支付系统处理失败了，但响应也丢了。

如果业务代码直接认为超时就是失败，可能会错误取消订单。如果直接重试，又可能重复支付。

更稳妥的做法是：
- 使用业务请求号保证幂等。
- 超时后查询支付结果。
- 支付回调和主动查询都要能更新同一个状态机。
- 不根据一次网络超时直接做最终业务判断。

这就是分布式系统中最重要的思维：不知道结果时，不要假装知道。

#### 4. 故障检测可能误判
分布式系统通常通过心跳、超时、健康检查来判断节点是否存活。

但问题是：如果一个节点没有响应，它可能是真的宕机了，也可能只是：
- 网络暂时抖动。
- GC 暂停。
- CPU 被打满。
- 线程池耗尽。
- 磁盘 IO 卡住。
- 进程还活着，但响应很慢。

因此，故障检测本质上是不可靠的，只能基于超时做猜测。

Java 开发什么时候需要关心？

当你使用服务注册发现、数据库主从切换、Redis Sentinel、分布式锁、定时任务调度时，都在依赖故障检测。如果系统误判某个节点死亡，就可能触发错误切换、重复执行或脑裂。

例如旧主库只是网络暂时不可达，但它并没有停止。如果新主库被选出来，同时旧主库还继续接受写入，就可能出现两个主库，这就是脑裂风险。

#### 5. 进程暂停
进程暂停指程序不是崩溃，而是暂时无法执行。对 Java 程序来说，比较典型的是 GC Stop-The-World 暂停。

暂停期间，进程看起来像“失联”了：
- 不响应心跳。
- 不处理请求。
- 不续约锁。
- 不消费消息。
- 不执行定时任务。

如果暂停时间超过外部系统的超时阈值，其他节点可能认为它已经死了。

一个典型例子是 Redis 分布式锁：

```java
boolean locked = redisLock.tryLock("job:daily", 30, TimeUnit.SECONDS);
```

如果线程拿到 30 秒锁后，因为 GC、阻塞 IO 或下游慢调用，实际执行了 60 秒，那么锁在第 30 秒已经过期，其他实例可能拿到同一把锁并开始执行同一个任务。此时两个 Java 进程会同时认为自己持有锁。

所以使用分布式锁时，不能只依赖“我加锁了”这个事实，还要考虑：
- 锁是否会过期。
- 业务是否可能执行超时。
- 是否需要自动续期。
- 是否有 fencing token 防止旧持有者继续写入。
- 业务是否具备幂等能力。

#### 6. 时钟不可靠
分布式系统中，每台机器都有自己的时钟。不同机器的时钟可能不完全一致，也可能因为 NTP 校准发生跳变。

这会影响很多看似普通的 Java 逻辑：
- 根据本地时间判断锁是否过期。
- 根据时间戳判断哪个写入更新。
- 使用 `System.currentTimeMillis()` 计算耗时。
- 分布式任务按时间窗口处理数据。
- token、验证码、订单超时关闭依赖服务器时间。

如果不同机器时钟相差较大，就可能出现奇怪问题。例如 A 机器生成的 token 在 B 机器上被认为已经过期；某个订单刚创建，却因为时间判断异常被定时任务关闭。

实践上：
- 计算耗时优先使用单调时钟，例如 `System.nanoTime()`。
- 不要把不同机器的本地时间当成绝对一致。
- 依赖时间排序的业务要谨慎。
- 关键系统要保证 NTP 正常。
- 分布式锁和租约不要只靠客户端本地时间判断。

#### 7. 最后写入获胜的风险
很多系统会用时间戳来解决冲突：谁的时间戳更新，就保留谁。这叫最后写入获胜。

它简单，但有风险。因为不同机器时钟可能不准，后发生的写入不一定有更大的时间戳。

例如两个服务同时更新用户资料：
- A 机器时钟快 5 秒。
- B 机器时钟正常。
- B 的更新真实发生得更晚，但时间戳可能比 A 小。

如果只按时间戳决定最终结果，就可能丢掉真正较新的写入。

Java 开发在以下场景要特别小心：
- 多节点同时更新同一份缓存。
- 根据更新时间覆盖 Elasticsearch 文档。
- 多数据源同步时用更新时间判断新旧。
- MQ 消息乱序时按事件时间覆盖状态。

更稳妥的方式可能是使用版本号、递增序列、数据库约束、状态机流转规则，而不是单纯依赖服务器时间。

#### 8. 分布式锁不是万能的
很多 Java 项目会用 Redis 或 ZooKeeper 实现分布式锁，解决多实例重复执行问题。

但分布式锁只是一种协调手段，不是业务正确性的全部保障。

常见问题包括：
- 锁过期后业务还没执行完。
- 客户端拿到锁后发生长时间 GC。
- Redis 主从切换导致锁状态丢失。
- 加锁成功但释放锁时误删他人的锁。
- 网络分区导致不同节点对锁状态理解不一致。

所以关键业务不能只靠锁，还要加业务层兜底：
- 唯一索引。
- 状态机。
- 幂等表。
- fencing token。
- 版本号。
- 补偿任务。

例如定时关单任务，即使使用分布式锁，也应该在更新订单时加状态条件：

```sql
update orders
set status = 'CLOSED'
where id = ?
  and status = 'WAIT_PAY';
```

这样即使任务重复执行，也不会把已经支付的订单关闭。

#### 9. 重试会放大问题
分布式系统中，失败重试很常见。网络超时重试、MQ 消费失败重试、RPC 框架重试、定时任务重试，都可以提高成功率。

但重试也可能放大问题：
- 对方已经成功，重试导致重复执行。
- 下游已经过载，重试造成更大压力。
- 多层系统都重试，形成重试风暴。
- 请求没有幂等，导致重复扣款、重复发券、重复创建。

Java 开发什么时候应该关心？只要你的代码里有远程调用、MQ 消费、定时任务、补偿任务，就必须考虑重试。

更好的实践是：
- 设置最大重试次数。
- 使用指数退避。
- 区分可重试和不可重试错误。
- 核心操作设计幂等键。
- 下游过载时优先限流和熔断，而不是无限重试。

#### 10. 拜占庭故障
书中也提到拜占庭故障，也就是节点不仅可能失败，还可能撒谎、作恶或返回错误信息。

大多数普通 Java 业务系统不会直接处理拜占庭故障，因为内部服务通常假设节点不会恶意作假。我们更常面对的是崩溃故障、网络故障、超时和重复。

但在一些场景中，Java 开发需要有类似意识：
- 对外开放接口不能相信客户端参数。
- 支付回调必须验签。
- Webhook 必须校验来源和签名。
- 订单金额不能相信前端传值。
- 多方协作系统要有审计和防篡改能力。

所以虽然我们不一定实现拜占庭容错算法，但“不要完全信任外部输入”是同一类思想。

### Java 开发什么时候应该关心这些问题

#### 1. 调用外部服务时
只要调用第三方支付、短信、物流、风控、银行、OpenAPI，就要关心网络超时、重试、幂等和结果确认。

例如支付接口超时后，不能简单认为支付失败，也不能无脑重试。应该通过商户订单号保证幂等，并通过查询接口或回调确认最终状态。

#### 2. 使用 MQ 时
MQ 至少一次投递很常见，所以消费者必须假设消息可能重复、乱序、延迟。

例如发券消息重复消费时，如果没有幂等表或唯一约束，用户可能收到多张券。积分消息重复消费时，用户积分可能重复增加。

#### 3. 使用 Redis 分布式锁时
只要用 Redis 锁控制定时任务、库存、活动资格、资源抢占，就要考虑锁过期、自动续期、误释放、GC 暂停和业务幂等。

锁只能减少并发概率，不能替代数据库条件更新和业务状态校验。

#### 4. 写定时任务时
Java 应用多实例部署后，定时任务可能在多个节点同时执行。即使用了 XXL-JOB、Quartz、ShedLock 或自研锁，也要保证任务重复执行是安全的。

例如批量关单、补偿发货、重试退款，都要通过状态条件、幂等记录和执行日志保证重复执行不会破坏数据。

#### 5. 做缓存一致性时
缓存、数据库、MQ、主从复制之间都有延迟。写数据库后删缓存、读从库后回填缓存、消息延迟更新缓存，都可能产生旧数据。

这时要关心复制延迟、消息延迟、并发读写和版本覆盖，而不是只看单行代码逻辑。

#### 6. 做服务治理时
如果一个下游服务变慢，Java 应用可能出现线程池耗尽、连接池耗尽、请求堆积、雪崩。

所以要关心：
- 超时。
- 限流。
- 熔断。
- 隔离。
- 降级。
- 舱壁模式。

这不是纯中间件问题，而是每个服务调用方都要设计的防御能力。

#### 7. 做分布式事务或最终一致时
Seata、TCC、本地消息表、可靠消息、Saga 都建立在“远程调用可能失败、重复、乱序”的前提上。

如果不理解这些故障模型，就很容易以为框架能自动解决所有问题，忽略幂等、空回滚、悬挂、补偿和对账。

#### 8. 做多机房或跨地域系统时
跨地域网络延迟更高，故障概率更大，时钟和复制延迟问题更明显。

如果业务要求异地多活、跨机房写入、灾备切换，就必须关心网络分区、数据冲突、故障切换和一致性取舍。

### 架构中的主要权衡

#### 可用性 vs 正确性
系统可以在网络异常时继续提供服务，但可能会接受短暂不一致。也可以为了正确性拒绝部分请求，但可用性会下降。支付、余额、库存更偏正确性，推荐、日志、通知更偏可用性。

#### 超时时间短 vs 误判概率高
超时设置太短，容易把慢请求误判为失败，引发重试和故障切换。超时设置太长，又会让线程和连接长时间占用。超时应该结合下游性能、业务容忍度和容量保护一起设计。

#### 重试提高成功率 vs 重试风暴
适当重试可以处理偶发网络抖动，但无节制重试会放大故障。重试必须配合幂等、退避、限流和熔断。

#### 分布式锁简化协调 vs 锁失效风险
分布式锁能减少多实例并发执行，但锁过期、GC 暂停、网络分区、主从切换都可能让锁失效。关键业务必须用数据库约束和状态机兜底。

#### 自动故障切换 vs 脑裂风险
自动切换能缩短恢复时间，但错误判断可能导致多个节点同时认为自己是主节点。系统越自动化，越需要明确故障模型和保护机制。

### 对日常 Java 开发的实际帮助

#### 1. 不再把远程调用当成本地调用
`userService.getUser()` 如果是本地方法，失败很明确；但 `userClient.getUser()` 如果是远程调用，就必须考虑超时、重试、降级、幂等和未知结果。

代码风格上看起来都是方法调用，但可靠性模型完全不同。

#### 2. 所有外部写操作都要有业务请求号
支付、退款、发券、创建订单、加积分、扣库存，只要可能重试，就应该有业务唯一号。

这个唯一号可以用于：
- 防重复提交。
- 查询最终结果。
- 消费幂等。
- 对账和排查。

#### 3. 状态机比布尔字段更可靠
分布式环境下，业务状态可能被多个异步事件更新。用明确状态机比简单布尔字段更安全。

例如订单状态应该规定：
- `WAIT_PAY` 可以变成 `PAID` 或 `CLOSED`。
- `PAID` 不能再变成 `CLOSED`。
- `CLOSED` 不能再支付成功。

更新 SQL 也要带状态条件，避免乱序消息破坏状态。

#### 4. 幂等是 Java 后端基本功
幂等不是只有支付才需要。只要存在重试、MQ、定时任务、多实例并发，就需要幂等。

常见方式包括：
- 唯一索引。
- 幂等表。
- 请求流水号。
- 状态机判断。
- 乐观锁版本号。
- 去重缓存。

#### 5. 监控比猜测重要
分布式问题很难只靠日志猜。需要监控：
- RPC 延迟和错误率。
- 线程池队列。
- 数据库连接池。
- Redis 延迟。
- MQ 堆积。
- GC 暂停时间。
- 机器时钟同步。
- 下游依赖健康状态。

很多所谓业务异常，背后其实是延迟、超时、堆积和资源耗尽。

### 实践举例

#### 1. 支付超时
用户点击支付，Java 服务调用支付渠道超时。

错误做法：
- 直接把订单改成支付失败。
- 立即用同一个请求重复扣款。

更好的做法：
- 使用商户订单号保证支付请求幂等。
- 订单保持 `PAYING` 状态。
- 等待支付回调或主动查询结果。
- 回调和查询都通过状态机更新订单。
- 定时任务补偿长期未知的订单。

#### 2. MQ 重复消费
订单创建后发送“发放积分”消息，消费者可能收到两次。

错误做法：
- 每消费一次就直接给用户加积分。

更好的做法：
- 用消息 ID 或订单 ID 建唯一消费记录。
- 积分流水表加唯一索引。
- 消费失败可以重试。
- 重复消息直接返回成功。

#### 3. Redis 锁过期
定时任务拿到 Redis 锁后处理大批量数据，结果执行时间超过锁过期时间，另一台机器也拿到锁开始执行。

错误做法：
- 认为只要加了 Redis 锁就不会重复。

更好的做法：
- 控制单次任务执行时间。
- 必要时使用自动续期。
- 每条数据更新时带状态条件。
- 任务处理记录做幂等。
- 允许重复执行但结果不重复生效。

#### 4. 下游服务变慢
订单服务调用库存服务，库存服务突然响应变慢。如果订单服务没有超时和隔离，线程会大量阻塞，最终订单服务也不可用。

更好的做法：
- 设置合理超时。
- 使用线程池隔离或舱壁模式。
- 对非核心能力降级。
- 对核心能力限流排队。
- 错误率升高时熔断。

#### 5. 定时关单和支付成功乱序
订单 30 分钟未支付会被定时任务关闭。但用户可能刚好在关单任务执行时完成支付。

错误做法：
- 定时任务直接把订单状态改为关闭。

更好的做法：

```sql
update orders
set status = 'CLOSED'
where id = ?
  and status = 'WAIT_PAY';
```

支付成功也要类似：

```sql
update orders
set status = 'PAID'
where id = ?
  and status = 'WAIT_PAY';
```

最终谁先成功，状态就进入对应终态，另一个操作影响行数为 0，不再错误覆盖。

### 我的理解
- 分布式系统的麻烦不是只有架构师、运维或中间件团队才需要关心，Java 业务代码最终也会受到影响。
- 网络超时不代表对方失败，只代表调用方不知道结果。
- 远程调用和本地方法调用看起来相似，但可靠性模型完全不同。
- 节点失联不一定是宕机，也可能是网络抖动、GC 暂停、CPU 打满或线程池耗尽。
- 时钟不能被当成绝对可靠的全局事实，跨机器比较时间要谨慎。
- 分布式锁、重试、自动故障切换都不是银弹，用不好反而会制造重复和不一致。
- Java 开发真正要落地的是超时、幂等、状态机、重试退避、熔断降级、监控告警和补偿对账。

### 可落地的实践启发
- 所有远程调用都必须设置超时时间。
- 外部写接口必须有业务唯一请求号。
- 超时后不要直接判断业务失败，要设计结果查询或补偿。
- MQ 消费者默认消息会重复，必须实现幂等。
- 定时任务默认可能重复执行，必须让重复执行安全。
- Redis 分布式锁只能作为协调手段，不能替代业务状态校验。
- 核心状态更新要带前置状态条件，避免乱序覆盖。
- 重试要限制次数，并使用退避策略。
- 下游依赖要配置熔断、限流、隔离和降级。
- 使用时间戳判断新旧时要考虑机器时钟不一致。
- 关键系统要监控 GC、线程池、连接池、RPC 延迟、MQ 堆积和依赖错误率。
- 分布式事务、TCC、Saga、本地消息表都必须配合幂等和补偿。

### 一句话总结
分布式系统的麻烦并不只是网络、机器、时钟这些底层问题，而是它们最终会表现为 Java 业务中的超时、重复、乱序、锁失效和状态不一致；真正可靠的后端系统，必须默认远程调用不可靠，并用幂等、状态机、超时、重试、熔断和补偿来保护业务正确性。


