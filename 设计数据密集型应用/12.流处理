# 《设计数据密集型应用》读书笔记

## 第 12 章：流处理

### 本章核心
这一章讲的是流处理，也就是面对不断产生、持续到达的数据，系统如何实时或准实时地处理它们。批处理处理的是一批有边界的数据，流处理处理的是没有明显终点的数据流。

对 Java 后端开发来说，流处理并不只是 Flink、Spark Streaming、Kafka Streams 这类大数据框架。更常见的形式包括：MQ 消费、订单事件处理、支付回调处理、数据库 binlog 同步、缓存更新、搜索索引同步、日志实时分析、SSE 流式响应、WebSocket 推送、响应式流处理等。

如果第 13 章讲的是流式系统的哲学，也就是用事件驱动和事件日志重新理解系统状态，那么第 12 章更偏工程实现：事件来了以后，如何消费、如何处理、如何保证顺序、如何处理重复、如何做窗口统计、如何容错、如何让结果最终正确。

### 我读完后的直观感受
这一章刚开始可能容易和批处理混在一起。批处理是每天凌晨处理一批历史数据，流处理是事件一发生就持续处理。

在 Java 系统里，其实流处理非常常见，只是我们平时不一定这么叫。比如：
- RocketMQ 消费订单支付成功消息。
- Kafka 消费用户行为日志。
- Canal 订阅 MySQL binlog 同步到 Elasticsearch。
- 支付成功后异步发积分。
- 库存变更后实时刷新缓存。
- 后台页面通过 WebSocket 实时展示任务进度。
- ChatGPT 这类接口通过 SSE 流式返回内容。
- 风控系统实时消费交易事件计算风险。

这些都是流处理思想在 Java 中的落地。

第 13 章让我理解“事件可以替代一部分刚性事务”，第 12 章则让我理解：一旦系统使用事件驱动，就必须认真面对事件流的处理问题，包括重复、乱序、延迟、失败、重放、状态维护和最终一致。

### 关键概念

#### 1. 什么是流
流是一组持续产生的数据记录。

例如：
- 用户点击事件流。
- 订单状态变化事件流。
- 支付回调事件流。
- 数据库变更日志流。
- 设备上报指标流。
- 应用日志流。
- MQ 消息流。

流和普通集合的区别是：集合通常是有限的，流可以无限持续。你不能等所有数据都到齐再处理，因为数据永远不会真正结束。

这要求系统采用持续消费、持续计算、持续输出的模式。

#### 2. 流处理和批处理的区别
批处理处理的是有界数据集，例如昨天所有订单、某个账期的账单、某个文件里的记录。

流处理处理的是无界数据流，例如不断到来的订单事件、支付事件、日志事件。

可以简单理解：
- 批处理：先积累，再集中处理。
- 流处理：事件到达后持续处理。

批处理适合：
- 对账。
- 历史修复。
- 离线报表。
- 全量索引重建。
- 数据迁移。

流处理适合：
- 实时通知。
- 实时风控。
- 实时统计。
- 缓存更新。
- 搜索索引同步。
- 事件驱动业务流程。

真实系统里两者经常结合：流处理负责实时响应，批处理负责兜底校准。

#### 3. 消息队列是 Java 中最常见的流处理入口
Java 后端最常接触的流处理形式就是 MQ。

例如订单支付成功后发送消息：

```text
OrderPaidEvent
```

积分服务、通知服务、搜索同步服务分别消费这个事件：

```text
OrderPaid -> 发积分
OrderPaid -> 发短信
OrderPaid -> 更新 ES
OrderPaid -> 更新报表
```

这就是事件流处理。订单服务不再同步调用所有下游，而是把业务事实写入事件流，由不同消费者各自处理。

这种设计的优点：
- 解耦生产者和消费者。
- 降低主流程延迟。
- 下游失败可以重试。
- 新增消费者不需要改主流程。
- 支持异步最终一致。

缺点：
- 消息可能重复。
- 消息可能乱序。
- 消费可能失败。
- 状态可能短暂不一致。
- 排查链路更复杂。

#### 4. 事件生产者和消费者
流处理系统中通常有生产者和消费者。

生产者负责产生事件，例如：
- 订单服务产生 `OrderCreated`。
- 支付服务产生 `PaymentSucceeded`。
- 用户服务产生 `UserRegistered`。
- Canal 产生数据库变更事件。

消费者负责处理事件，例如：
- 库存服务消费订单创建事件。
- 积分服务消费订单支付事件。
- 搜索服务消费订单变更事件。
- 报表服务消费用户行为事件。

生产者和消费者通过消息系统解耦。生产者只需要表达“发生了什么”，消费者决定“收到这个事实后我要做什么”。

这和第 13 章的事件驱动哲学是一致的：事件是业务事实，状态由事件推动变化。

#### 5. 至少一次、至多一次、恰好一次
流处理里经常讨论消息投递语义。

至多一次指消息最多处理一次，可能丢失，但不会重复。适合不重要的指标、日志、监控采样。

至少一次指消息不会轻易丢失，但可能重复。这是 Java 业务中最常见的模式。RocketMQ、Kafka、RabbitMQ 消费时，如果处理失败或确认失败，消息可能再次投递。

恰好一次听起来最理想，但实现成本高，而且通常只在特定系统边界内成立。业务层仍然要面对重复调用、数据库写入、外部接口幂等问题。

对 Java 后端最重要的结论是：默认 MQ 会重复投递，消费者必须幂等。

#### 6. 幂等消费
幂等是流处理的基础能力。

例如积分服务消费 `OrderPaid`，不能每收到一次消息就直接加积分：

```sql
update user_account set points = points + 100 where user_id = ?;
```

如果消息重复，积分会重复增加。

更好的做法是写积分流水，并加唯一约束：

```text
unique(order_id, point_type)
```

消费流程：
- 收到 `OrderPaid`。
- 尝试插入积分流水。
- 如果插入成功，加积分。
- 如果唯一键冲突，说明已经处理过，直接返回成功。

常见幂等手段包括：
- 消息 ID 去重。
- 业务单号唯一索引。
- 消费记录表。
- 状态机条件更新。
- 乐观锁版本号。
- 输出覆盖而不是追加。

#### 7. 顺序与乱序
流处理系统中，顺序非常重要，但也很难保证全局顺序。

比如订单事件理论顺序是：

```text
OrderCreated -> OrderPaid -> OrderShipped -> OrderCompleted
```

但消费者可能因为网络、重试、分区、不同 topic、不同消费者组，看到乱序事件。

常见处理方式包括：
- 同一业务 key 发送到同一分区。
- 消费者按业务 key 串行处理。
- 事件中携带版本号或序列号。
- 状态更新带前置状态条件。
- 对缺失前置事件进行等待或补偿。

Java 业务中最实用的是状态机保护。例如：

```sql
update orders
set status = 'PAID'
where id = ?
  and status = 'WAIT_PAY';
```

如果 `OrderPaid` 比 `OrderCreated` 先到，或者订单状态已经被取消，更新就不会错误覆盖。

#### 8. 事件时间与处理时间
流处理里有两个很重要的时间概念。

事件时间是事件真实发生的时间。例如用户实际支付成功的时间。

处理时间是系统处理事件的时间。例如消费者收到并处理消息的时间。

两者可能不同。支付 10:00 成功，但消息 10:05 才被处理。如果做实时统计，使用事件时间和处理时间会得到不同结果。

Java 业务中也会遇到类似问题：
- 订单超时关闭要看订单创建时间，不是任务处理时间。
- 支付成功要看渠道支付时间，也要记录本地接收时间。
- 报表统计要明确按业务发生时间还是系统入库时间。
- 风控规则要明确事件真实发生顺序。

所以事件中应该包含 `occurredAt`，同时系统也可以记录 `receivedAt` 和 `processedAt`。

#### 9. 窗口计算
流处理经常需要在一段时间窗口内聚合数据。

例如：
- 最近 1 分钟接口错误率。
- 最近 5 分钟支付成功率。
- 每小时订单金额。
- 每天用户活跃数。
- 10 秒内同一用户失败登录次数。

窗口可以按处理时间，也可以按事件时间。

在普通 Java 系统里，简单窗口可以用 Redis 实现，例如滑动窗口限流、登录失败计数。复杂窗口统计通常交给 Flink、Kafka Streams、ClickHouse 或实时数仓。

#### 10. 流处理中的状态
流处理不是无状态的。很多流处理任务都需要维护状态。

例如：
- 统计某个用户最近 10 分钟下单次数。
- 判断支付事件是否已经处理过。
- 维护订单当前状态。
- 聚合某个商品的实时销量。
- 记录每个分区消费到的 offset。

这些状态需要可靠保存，否则程序重启后就会丢失进度或重复处理。

在 Java 业务中，状态通常放在：
- 数据库。
- Redis。
- MQ offset。
- 消费记录表。
- 流处理框架状态后端。

这和第 13 章讨论的无状态 Web 服务并不冲突。应用实例可以是无状态的，但流处理任务的业务状态必须外置化或由框架托管。

#### 11. 重放
流处理系统的一个重要能力是重放历史事件。

如果 Kafka 中保留了订单事件，那么新增一个报表系统时，可以从历史 offset 开始消费，构建自己的报表视图。

重放也可以用于：
- 修复下游数据。
- 重建 Elasticsearch 索引。
- 重新计算报表。
- 验证新规则。
- 补偿失败消费者。

但重放要求消费者幂等。如果消费者不能重复处理同一事件，重放就会非常危险。

#### 12. CDC 与流处理
CDC 是数据库变更捕获，是 Java 系统中非常常见的流处理来源。

例如 MySQL binlog 里记录了订单表的变更。Canal、Debezium、Flink CDC 可以把这些变更转换成事件流。

常见用途：
- MySQL 同步到 Elasticsearch。
- MySQL 同步到 Redis 缓存。
- MySQL 同步到数据仓库。
- 监听订单状态变化触发下游处理。

CDC 的优点是与数据库提交顺序比较接近，不需要业务代码显式发消息。缺点是它表达的是数据变化，不一定是清晰的业务事件。

例如 `orders.status` 从 `WAIT_PAY` 改为 `PAID`，CDC 能捕获字段变化，但下游理解起来不如 `OrderPaid` 这个领域事件清晰。

所以可以这样选择：
- 业务协作用领域事件。
- 数据同步用 CDC。
- 关键一致性用 Outbox 或本地消息表。

#### 13. 流式响应也是一种流处理
Java Web 中还有一种常见的“流式处理”：服务端不是一次性返回完整结果，而是持续向客户端返回数据。

常见技术包括：
- SSE。
- WebSocket。
- Spring WebFlux。
- Servlet Streaming。
- gRPC streaming。

例如 AI 问答接口，模型不是等完整回答生成后一次性返回，而是一段一段返回 token：

```text
data: 你好

data: ，我可以

data: 帮你分析
```

这种流式响应可以提升用户体验，因为用户更早看到结果。

这和 MQ 流处理不是同一个层面，但思想相通：系统不一定要等待全部结果完成后再输出，可以边产生、边处理、边返回。

### 架构中的主要权衡

#### 实时性 vs 正确性
流处理追求低延迟，但事件可能乱序、重复和迟到。越追求实时，越要接受中间结果可能暂时不准确。关键业务需要状态机和补偿兜底。

#### 解耦 vs 可观测性
事件流可以解耦服务，但链路更长，排查更难。必须有 traceId、eventId、消费日志、死信队列和监控告警。

#### 高吞吐 vs 顺序保证
并行消费可以提高吞吐，但会牺牲顺序。按业务 key 串行处理可以保证局部顺序，但吞吐受限。通常要保证同一订单、同一用户、同一账户维度的局部顺序，而不是全局顺序。

#### 至少一次 vs 幂等成本
至少一次投递能降低丢消息风险，但要求消费者幂等。幂等会增加表设计、唯一索引、消费记录和状态机复杂度。

#### 领域事件 vs CDC
领域事件语义清楚，适合业务协作；CDC 对业务代码侵入小，适合数据同步。两者可以结合，但不要混淆。

### Java 中的具体案例

#### 1. 支付成功后发积分
这是最典型的业务事件流处理。

流程：
1. 支付回调到订单服务。
2. 订单服务本地事务更新订单为 `PAID`。
3. 同一事务写入 `OrderPaid` 到 outbox 表。
4. 投递器把 `OrderPaid` 发到 MQ。
5. 积分服务消费消息。
6. 积分服务插入积分流水，唯一键为 `order_id + point_type`。
7. 插入成功后增加积分。
8. 插入失败说明已处理，直接返回成功。

这个案例体现了三点：
- 流处理承接跨服务后置动作。
- 消费者必须幂等。
- 事件驱动实现柔性事务。

如果积分服务挂了，订单支付状态不受影响；积分消息后续重试即可。

#### 2. 订单同步到 Elasticsearch
订单状态变化后，需要让运营后台能搜索。

同步链路：
1. 订单服务产生 `OrderCreated`、`OrderPaid`、`OrderShipped` 等事件。
2. 搜索同步服务消费订单事件。
3. 根据订单 ID 查询订单最新数据。
4. 使用订单 ID 覆盖写入 ES 文档。
5. 失败后重试，长期失败进入死信。
6. 定期批处理校验 MySQL 和 ES 数量及抽样一致性。

这里 ES 是读模型，不是事实来源。

设计重点：
- ES 写入用覆盖更新，天然更幂等。
- 乱序事件到达时，最好查询 MySQL 最新状态再写 ES。
- 如果 ES 长时间失败，不能影响订单主流程。
- 批处理负责最终校准。

这个案例把第 12 章流处理、第 11 章批处理、第 13 章事件驱动连接起来了。

#### 3. Canal 监听 MySQL binlog 刷新缓存
很多 Java 系统会用 Canal 监听 MySQL binlog，然后更新 Redis 缓存。

流程：
1. 商品表发生变更。
2. MySQL 写入 binlog。
3. Canal 解析 binlog。
4. 缓存同步服务收到商品变更事件。
5. 删除或更新 Redis 缓存。

优点：
- 业务代码不需要到处写删缓存逻辑。
- 变更基于数据库提交结果。
- 多个下游都可以订阅数据变化。

风险：
- binlog 同步有延迟。
- 消息可能重复。
- 缓存更新可能失败。
- 表结构变化会影响解析。

实践建议：
- 优先删除缓存，而不是复杂更新缓存。
- 缓存要设置过期时间。
- 消费失败要重试。
- 重要缓存要支持主动重建。

#### 4. 实时风控
支付系统或交易系统经常需要实时风控。

事件流：
```text
UserLoginEvent
OrderCreatedEvent
PaymentRequestedEvent
PaymentSucceededEvent
CouponUsedEvent
```

风控服务消费这些事件，维护用户近期行为状态，例如：
- 5 分钟内下单次数。
- 10 分钟内支付失败次数。
- 1 小时内使用优惠券次数。
- 当前设备关联账号数量。

如果规则触发，就输出风控结果。

实现方式：
- 简单场景可以用 Redis 计数和过期时间。
- 复杂场景可以用 Flink 或 Kafka Streams。
- 关键决策要记录规则命中日志。

这个案例说明流处理不仅是异步通知，也可以参与实时业务判断。

#### 5. 后台任务进度流式返回
Java Web 中常见一个场景：用户提交一个大任务，比如批量导入 Excel、批量生成报表、AI 文档分析，后端需要持续返回进度。

传统方式是前端轮询：
```text
GET /task/progress?id=xxx
```

流式响应方式可以用 SSE：

```java
@GetMapping(value = "/tasks/{id}/events", produces = MediaType.TEXT_EVENT_STREAM_VALUE)
public SseEmitter streamTask(@PathVariable Long id) {
    SseEmitter emitter = new SseEmitter(0L);
    taskProgressService.subscribe(id, emitter);
    return emitter;
}
```

后端任务每完成一批，就推送进度：

```text
progress: 10%
progress: 35%
progress: 80%
completed
```

这个例子属于响应流处理。它不一定改变数据库一致性模型，但体现了流式系统的输出思想：结果不是一次性返回，而是持续产生、持续推送。

设计注意：
- 连接断开要清理 emitter。
- 任务进度仍然要落数据库或 Redis，不能只存在内存。
- 客户端断线重连后要能恢复当前进度。
- 大量连接时要注意线程和连接资源。

#### 6. AI 接口流式返回
现在 Java 后端经常会接入大模型接口，返回方式通常是 SSE 流。

流程：
1. 前端发起提问。
2. Java 服务调用模型 API。
3. 模型持续返回 token。
4. Java 服务边接收边通过 SSE 转发给前端。
5. 回答结束后保存完整内容。

这种模式的好处是用户能更快看到结果，而不是等几十秒后一次性返回。

设计重点：
- 前端连接断开时要取消下游请求。
- 后端要处理模型流中断。
- 最终完整回答要持久化。
- 流式片段和最终状态要区分。
- 长连接要有超时和限流。

这个案例说明，流式处理不仅存在于后端异步消息，也存在于用户交互层。

#### 7. WebSocket 实时通知
例如订单状态变化后，后台页面希望实时看到。

流程：
1. 订单服务发布 `OrderStatusChanged`。
2. 通知服务消费事件。
3. 通知服务查找订阅该订单或商家的在线用户。
4. 通过 WebSocket 推送状态变化。

这里 MQ 是服务间事件流，WebSocket 是服务到客户端的流式通道。

设计注意：
- WebSocket 推送不能作为唯一事实来源。
- 客户端收到推送后最好再按需查询最新状态。
- 推送失败不应影响订单主流程。
- 在线连接状态要和业务状态区分。

### 设计思路

#### 1. 先判断这是批处理还是流处理
如果数据有明确边界，例如昨天所有订单，优先批处理。

如果数据持续产生，并且需要及时响应，例如支付成功、订单状态变化、风控事件，优先流处理。

#### 2. 事件要有清晰语义
事件应该表达业务事实，而不是模糊的数据变化。

推荐：
- `OrderPaid`
- `RefundSucceeded`
- `UserRegistered`
- `InventoryReserved`

不推荐：
- `DataChanged`
- `OrderUpdated`
- `TableModified`

除非是 CDC 场景，否则事件越贴近业务语义，消费者越容易正确处理。

#### 3. 设计事件结构
一个事件最好包含：
- eventId。
- eventType。
- aggregateId。
- occurredAt。
- version。
- traceId。
- producer。
- payload。

这些字段能帮助幂等、追踪、重放、兼容和排查。

#### 4. 默认事件会重复、乱序、延迟
消费者设计时不要假设完美消息系统。

要提前设计：
- 幂等表。
- 状态机。
- 版本号。
- 死信队列。
- 重试策略。
- 告警。
- 补偿任务。

#### 5. 同一业务对象尽量保证局部顺序
不需要追求全局顺序，但同一订单、同一账户、同一用户的事件最好进入同一个分区。

例如 Kafka 可以用 `orderId` 作为 key，保证同一订单事件进入同一 partition。

这样可以降低乱序处理复杂度。

#### 6. 关键状态仍然落数据库
流处理可以推动状态变化，但核心业务状态不能只存在 MQ 或消费者内存中。

例如订单当前状态、退款状态、积分余额、库存状态，最终都应该落到数据库或可靠存储中。

事件流负责传播事实，数据库状态表负责支撑查询和约束。

#### 7. 流处理要配合批处理兜底
流处理追求实时，但不保证永远成功。批处理负责校准。

例如：
- 实时事件发积分。
- 每日批处理扫描漏发积分。
- 实时事件同步 ES。
- 每日批处理校验 ES 和 MySQL。
- 实时回调更新支付状态。
- 对账任务确认渠道最终结果。

这也是第 12 章和第 13 章结合后的核心设计：实时事件驱动业务推进，批处理保证最终收敛。

### 我的理解
- 流处理是事件驱动系统的工程基础，事件驱动不是发一条 MQ 就结束，而是要持续、可靠地消费事件流。
- 对 Java 开发来说，MQ 消费、CDC 同步、缓存刷新、ES 同步、实时通知、SSE 流式响应，本质上都可以放到流处理框架下理解。
- 流处理解决的是“事件持续发生时，系统如何及时反应”；第 13 章解决的是“为什么可以用事件驱动状态变化”。
- 流处理一定要面对重复、乱序、延迟、失败和重放，所以幂等、状态机、offset、死信、补偿非常重要。
- 事件流可以替代一部分同步调用和刚性事务，但不能替代业务正确性设计。
- Java Web 服务可以保持无状态，但流处理中的业务状态要外置到数据库、Redis、消费记录或流处理框架状态后端。
- 流式响应是流处理思想在接口层的体现，适合任务进度、实时通知、AI token 输出等场景。

### 可落地的实践启发
- MQ 消费者默认消息会重复，必须做幂等。
- 同一业务对象的事件尽量使用同一个 message key，保证局部顺序。
- 状态更新要带前置状态条件，防止乱序事件覆盖正确状态。
- 事件中要包含 `eventId`、`occurredAt`、`traceId`、`version` 等字段。
- 业务协作优先使用领域事件，数据同步可以使用 CDC。
- 订单、支付、积分、退款这类关键事件要有死信、告警和补偿任务。
- ES、缓存、报表通常是派生视图，失败不应影响核心事务。
- 流处理要配合批处理对账，保证最终一致。
- SSE、WebSocket、WebFlux 适合流式响应，但核心进度和最终结果仍要落库。
- 不要把 MQ 当成万能事务工具，事件驱动仍然需要状态机和幂等兜底。

### 一句话总结
流处理的核心，是面对持续到来的事件流，让系统能够及时、可靠、可恢复地做出反应；对 Java 开发来说，它最常见的落地不是手写大数据框架，而是 MQ 消费、CDC 同步、缓存和 ES 更新、实时风控、SSE/WebSocket 流式响应，并通过幂等、状态机、死信、重试和批处理补偿，支撑第 13 章所说的事件驱动和柔性事务。
