# 《设计数据密集型应用》读书笔记

## 第 11 章：批处理

### 本章核心
这一章讲的是批处理，也就是一次性处理大量已有数据，而不是像在线接口那样一个请求处理一次。批处理通常不追求毫秒级响应，而是关注吞吐、可靠性、可重跑、可恢复、可观察和最终结果正确。

对 Java 后端开发来说，批处理不一定表现为 Hadoop、MapReduce、Spark 这些大数据框架。更多时候，它表现为定时任务、数据修复脚本、对账任务、补偿任务、批量导入导出、索引重建、历史数据迁移、离线报表生成。

所以这一章对 Javaer 是有意义的，只是不能只按“大数据工程师”的视角读，而要把它理解成：当系统需要处理大量历史数据、异步补偿失败操作、修复线上数据、生成报表和对账时，应该如何安全、可控地处理。

### 我读完后的直观感受
刚开始看批处理，可能会觉得离日常 Java 业务开发有点远。因为很多 Javaer 平时主要写的是接口、事务、缓存、MQ、RPC，很少直接写 MapReduce 或 Spark 任务。

但仔细想，Java 项目里其实到处都是批处理：
- 每天凌晨关闭超时订单。
- 每天和支付渠道对账。
- 批量补发失败消息。
- 批量修复历史脏数据。
- 批量导入 Excel。
- 批量导出账单。
- 批量重建 Elasticsearch 索引。
- 批量同步第三方系统数据。
- 批量清理过期数据。
- 批量生成统计报表。

这些任务如果数据量小，看起来只是一个简单循环；但数据量一大，就会出现很多问题：内存爆掉、事务太大、数据库压力过高、任务中途失败、重复执行、无法恢复、处理进度不可见、线上业务被拖慢。

所以这一章最有价值的地方，不是让我一定去学 Hadoop，而是让我意识到：批处理是生产系统里非常重要的“兜底能力”。实时链路负责快速响应，批处理负责对账、补偿、修复、迁移和校准。

### 关键概念

#### 1. 什么是批处理
批处理是对一批已有数据进行离线或异步处理。

它和在线请求最大的区别是：
- 在线请求面向单个用户操作，要求低延迟。
- 批处理面向大量数据，要求高吞吐和可恢复。

例如用户点击创建订单，这是在线请求；每天凌晨扫描所有超时未支付订单并关闭，这是批处理。

在线请求更关注：
- 响应时间。
- 接口可用性。
- 并发性能。
- 用户体验。

批处理更关注：
- 总处理量。
- 失败重试。
- 幂等。
- 断点续跑。
- 资源控制。
- 处理结果校验。

#### 2. 批处理不是简单 for 循环
很多 Java 开发第一次写批处理，容易写成：

```java
List<Order> orders = orderMapper.selectAllNeedFix();
for (Order order : orders) {
    fix(order);
}
```

这种写法在几百条数据时没问题，但面对几十万、几百万数据时就很危险。

问题包括：
- 一次性加载太多数据，导致内存占用过高。
- 单次任务执行时间太长，失败后不知道处理到哪里。
- 没有限速，可能压垮数据库。
- 没有幂等，重跑可能重复处理。
- 没有进度记录，无法观察和恢复。
- 没有错误隔离，一条数据失败可能影响全部任务。

所以批处理的难点不是循环，而是如何让循环在大量数据和失败场景下仍然安全。

#### 3. 输入、处理、输出
批处理可以抽象为三个阶段：输入、处理、输出。

输入可以来自：
- 数据库表。
- 日志文件。
- CSV、Excel。
- MQ 堆积消息。
- 对账文件。
- 对象存储。
- 搜索索引。

处理可以包括：
- 数据清洗。
- 格式转换。
- 聚合统计。
- 状态修复。
- 规则校验。
- 补偿调用。

输出可以写到：
- 数据库。
- 文件。
- MQ。
- Elasticsearch。
- 报表表。
- 第三方接口。

这个抽象很有用。写批处理任务时，不要一上来就写业务循环，而要先明确：输入边界是什么，处理逻辑是否幂等，输出是否可以重复写，失败后如何恢复。

#### 4. MapReduce 的思想
书中会讲到 MapReduce，它是经典的批处理模型。

Map 阶段负责把输入数据拆开并转换成中间结果，Reduce 阶段负责按 key 聚合中间结果。

虽然普通 Javaer 不一定直接写 MapReduce，但这个思想很有启发：
- 大任务可以拆成小任务。
- 数据可以分区并行处理。
- 中间结果可以落盘。
- 失败任务可以单独重跑。
- 聚合逻辑和数据分布有关。

例如统计每个用户的订单金额：
- Map：读取订单，输出 `userId -> amount`。
- Shuffle：按 `userId` 分组。
- Reduce：汇总每个用户的金额。

在 Java 业务系统里，即使不用 MapReduce，也可以借鉴这种思路，把大任务拆成多个小批次、多个分片、多个 worker 并行处理。

#### 5. 批处理和 Unix 思想
书中提到 Unix 工具链思想：一个程序做好一件事，通过标准输入输出把多个工具组合起来。

这个思想对 Java 开发也有启发：
- 每个处理步骤尽量职责单一。
- 中间结果最好可观察、可落地。
- 任务之间通过明确数据格式衔接。
- 不要把所有逻辑都塞进一个巨大 Job。

例如一个对账任务可以拆成：
- 下载渠道账单。
- 解析账单文件。
- 与本地流水比对。
- 生成差异单。
- 自动补偿可修复差异。
- 输出人工处理报表。

拆开后，每一步都更容易排查和重跑。

#### 6. 幂等
幂等是批处理中最重要的原则之一。

批处理任务一定会失败，也一定会重跑。如果同一条数据处理两次会产生两次效果，那任务就很危险。

例如积分补发任务：

错误做法是每次处理都直接：

```sql
update user_account set points = points + 100 where user_id = ?;
```

如果任务重跑，积分会重复增加。

更好的做法是写积分流水，并对业务单号建立唯一索引：

```text
unique(order_id, point_type)
```

这样重复执行时，数据库会阻止重复生效。

批处理中的幂等手段包括：
- 唯一索引。
- 业务单号。
- 处理记录表。
- 状态机条件更新。
- 版本号。
- 输出覆盖而不是追加。
- 先校验是否已处理。

#### 7. 断点续跑
批处理任务经常执行很久。如果中途失败，不能每次都从头开始，也不能不知道哪些数据已经处理。

断点续跑就是记录处理进度，让任务可以从失败位置继续。

常见方式包括：
- 记录最后处理的 ID。
- 记录最后处理的时间窗口。
- 每批数据生成任务分片记录。
- 每条数据记录处理状态。
- 成功和失败分别落表。

例如按 ID 扫描订单：

```sql
select * from orders
where id > #{lastId}
order by id asc
limit 1000;
```

每处理完一批，就更新 `lastId`。任务失败后，从上次成功的 `lastId` 继续。

但要注意，如果数据会被更新或插入，单纯依赖时间或 ID 也可能漏数据，需要结合业务场景设计扫描条件。

#### 8. 分批处理
批处理不要一次性处理所有数据，而要分批。

分批的好处：
- 降低内存压力。
- 降低单次事务大小。
- 方便失败重试。
- 方便控制数据库压力。
- 方便记录进度。

常见批大小可能是 100、500、1000、5000，具体要看数据复杂度、SQL 成本、下游承载能力。

批大小不是越大越好。太小会导致频繁 IO 和调度成本，太大会导致内存、锁和事务压力过大。

#### 9. 避免大事务
批处理最容易犯的错误之一，是用一个大事务包住所有数据。

例如一次修复 50 万条数据，如果放在一个事务中，可能导致：
- 锁持有时间过长。
- undo log、redo log 压力过大。
- 回滚成本极高。
- 主从延迟变大。
- 影响线上业务查询和写入。

更好的方式是小批量提交。每批处理成功后提交事务，失败时只回滚当前批次。

但小批量提交也意味着任务不是全局原子的，所以必须配合幂等和进度记录。

#### 10. 资源隔离
批处理任务往往会消耗大量数据库、CPU、内存、网络和磁盘资源。如果和在线业务混用同一套资源，可能影响用户请求。

常见问题包括：
- 大查询拖慢主库。
- 批量更新造成锁等待。
- 导出任务占满内存。
- 批量调用第三方接口触发限流。
- 重建索引影响线上搜索。

实践中可以：
- 在低峰期执行。
- 使用从库或分析库读取。
- 控制批大小和并发数。
- 对任务限速。
- 使用独立线程池。
- 任务和在线接口资源隔离。
- 对核心数据库设置保护阈值。

#### 11. 可观察性
批处理任务不像在线请求那样每次都有用户反馈。如果没有可观察性，任务失败可能很久才被发现。

批处理应该记录：
- 任务开始时间。
- 任务结束时间。
- 总数据量。
- 成功数量。
- 失败数量。
- 跳过数量。
- 当前进度。
- 失败原因。
- 重试次数。
- 处理耗时。

最好有任务执行记录表和告警机制。否则批处理就会变成“跑没跑完全靠看日志”。

#### 12. 批处理和流处理的区别
批处理处理的是一批已有数据，通常有明确边界。比如昨天的订单、某个账期的账单、某个 ID 范围内的数据。

流处理处理的是持续不断到来的数据，数据没有天然结束点。比如实时日志、实时订单事件、实时风控数据。

简单理解：
- 批处理：处理有限数据集。
- 流处理：处理无限数据流。

但两者不是完全割裂的。很多系统会同时使用：
- 实时链路负责快速处理。
- 批处理负责补偿和校准。

例如订单支付成功后，实时 MQ 给用户加积分；每天凌晨批处理再扫描订单和积分流水，找出漏加的记录进行补偿。

### 架构中的主要权衡

#### 吞吐量 vs 延迟
批处理通常追求高吞吐，可以接受较高延迟。在线接口追求低延迟，不一定能承受大批量处理。不要把大量批处理逻辑塞进用户请求链路。

#### 简单脚本 vs 可恢复任务
一次性小数据修复可以写简单脚本，但长期运行的批处理必须考虑进度、重试、幂等和监控。越靠近生产核心数据，越不能用临时脚本思维。

#### 大批量效率 vs 小批量安全
批量越大，吞吐可能越高，但失败回滚、锁持有和资源占用也更严重。小批量更安全，但总耗时可能更长。批大小要结合数据库压力和业务窗口调整。

#### 全量处理 vs 增量处理
全量处理简单，适合初始化和重建，但成本高。增量处理效率高，但要准确识别变化数据，避免漏处理。很多系统需要全量加增量结合。

#### 同步完成 vs 异步任务
小数据量可以同步处理，大数据量应该异步化。比如 Excel 导入超过一定行数后，不应该让 HTTP 请求一直等待，而应该提交任务，后台处理，前端查询进度。

### 对日常 Java 开发的实际帮助

#### 1. 更安全地写定时任务
定时任务不是简单加一个 `@Scheduled`。多实例部署后，任务可能重复执行；任务执行失败后，需要重试；任务处理大量数据时，需要分批和限速。

可以使用 XXL-JOB、Quartz、ElasticJob、PowerJob 等调度工具，但业务逻辑仍然要幂等。

#### 2. 更理解对账和补偿
实时接口、MQ、回调都有失败概率。对账和补偿是生产系统稳定性的最后防线。

支付、退款、发货、积分、库存这类业务，不能只依赖实时链路成功，必须有离线任务定期校准状态。

#### 3. 更谨慎写数据修复脚本
线上数据修复要有灰度意识：
- 先查数量。
- 小批量试跑。
- 备份关键数据。
- 记录处理日志。
- 支持回滚或反向修复。
- 确认影响范围后再扩大。

不要直接在线上跑一个全表更新。

#### 4. 更合理设计批量导入导出
Excel 导入导出是很典型的 Java 批处理场景。

导入时要考虑：
- 流式读取。
- 分批校验。
- 错误行记录。
- 异步处理。
- 进度展示。
- 重复数据处理。

导出时要考虑：
- 分页查询。
- 流式写文件。
- 限制最大导出量。
- 大文件异步生成。
- 下载链接过期。

#### 5. 更清楚批处理和数据库的关系
批处理很容易给数据库造成压力。尤其是全表扫描、深分页、大事务、批量更新、复杂聚合。

实践中要：
- 避免深分页。
- 使用 ID 游标扫描。
- 控制批大小。
- 尽量命中索引。
- 避免在主库高峰期跑重任务。
- 必要时使用从库、数仓或分析库。

#### 6. 更能理解搜索索引和缓存重建
重建 Elasticsearch 索引、刷新缓存、同步历史数据，本质都是批处理。不能只考虑功能跑通，还要考虑全量数据规模、失败重试、增量衔接和切换方案。

### 实践举例

#### 1. 批量关闭超时订单
订单 30 分钟未支付需要关闭。

错误做法：
- 一次性查出所有超时订单。
- 一个大事务全部关闭。
- 不判断订单当前状态。

更好的做法：
- 按 ID 或时间分批扫描。
- 每批控制数量。
- 更新时带状态条件。
- 关闭订单、释放库存都要幂等。
- 记录每批处理进度。

核心 SQL 类似：

```sql
update orders
set status = 'CLOSED'
where id = ?
  and status = 'WAIT_PAY';
```

这样即使支付成功和关单任务并发，也不会错误覆盖终态。

#### 2. 支付对账任务
每天凌晨拉取支付渠道账单，与本地支付流水对比。

处理步骤可以是：
- 下载账单文件。
- 解析渠道流水。
- 根据商户订单号匹配本地流水。
- 找出金额、状态、时间不一致的数据。
- 生成差异记录。
- 自动补偿可确认的数据。
- 需要人工判断的数据进入工单。

这个任务的意义是弥补实时支付回调、网络超时、MQ 失败带来的不确定性。

#### 3. 历史数据修复
某次上线导致部分订单缺少 `merchant_id`，需要修复历史数据。

安全做法：
- 先统计影响数据量。
- 选取少量数据试跑。
- 每批处理 500 或 1000 条。
- 修复前记录旧值。
- 修复后校验数量。
- 失败数据单独落表。
- 支持重复执行。

数据修复不是写完 SQL 就完事，而是一次小型生产变更。

#### 4. Excel 批量导入
运营上传 10 万行商品数据。

更好的设计：
- 上传文件后生成导入任务。
- 后台异步解析。
- 分批校验和写入。
- 每行记录成功或失败原因。
- 前端轮询任务进度。
- 最后生成错误报告。

这样比 HTTP 请求里同步处理更可靠，也更容易排查。

#### 5. Elasticsearch 索引重建
订单数据需要重建到 ES。

更好的做法：
- 创建新索引。
- 从 MySQL 按 ID 游标分批读取。
- 批量写入 ES。
- 失败批次记录并重试。
- 全量完成后追增量数据。
- 校验数量和抽样数据。
- 使用 alias 切换到新索引。

直接删除旧索引再全量写入风险很高，因为中途失败会影响线上查询。

### 我的理解
- 批处理不是过时技术，而是生产系统中处理历史数据、失败补偿、对账校准和数据迁移的重要能力。
- 对 Java 开发来说，不一定要深入 Hadoop 和 Spark，但必须掌握批量任务的工程原则。
- 批处理的核心不是循环处理，而是分批、幂等、断点续跑、失败重试、资源控制和可观察性。
- 实时链路解决用户请求的及时响应，批处理解决系统长期正确性和数据最终收敛。
- 很多线上事故不是因为没有批处理，而是批处理写得太随意：大事务、全表扫、不可重跑、无进度、无告警。
- 批处理任务越靠近核心业务数据，越要像正式服务一样设计，而不是当成一次性脚本。

### 可落地的实践启发
- 大量数据不要一次性加载到内存，要分批读取。
- 批处理任务必须支持幂等，默认会失败和重跑。
- 不要用一个大事务处理全部数据，优先小批量提交。
- 任务要记录进度，支持断点续跑。
- 每批处理要记录成功数、失败数和失败原因。
- 对数据库访问要控制批大小、并发数和执行时间。
- 高风险修复任务要先小范围试跑，再逐步扩大。
- 定时任务在多实例环境下要考虑重复执行。
- 对账、补偿、修复任务是实时链路的重要兜底。
- 大文件导入导出应该异步化，并提供进度和错误报告。
- 重建索引要考虑全量、增量、校验和切换，不要直接破坏旧数据。
- 批处理要有监控和告警，不能只靠人工看日志。

### 一句话总结
批处理对 Java 开发很有意义，只是它通常不以 Hadoop 或 MapReduce 的形式出现，而是藏在定时任务、对账、补偿、数据修复、导入导出和索引重建中；好的批处理不是简单循环，而是能在大量数据、失败重试和线上压力下安全、幂等、可恢复地完成任务。
